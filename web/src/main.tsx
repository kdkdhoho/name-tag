import React,{useState} from 'react'; import {createRoot} from 'react-dom/client'; import './style.css';
type P=any; const initial={home:'STUDIO',sizeLimit:'UNDER_10',tenure:'OWN',absence:'UNDER_4',housemates:['ALONE'],activeDays:3,experience:'NONE',expectation:'WEEKS',sido:'서울',sigungu:'강남구'};
function App(){const[p,setP]=useState<P>(initial),[result,setResult]=useState<any>(),[plan,setPlan]=useState<any>();const match=async()=>{let x=await fetch('/api/v1/matches',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(p)}).then(r=>r.json());setResult(x.data)};if(result?.landlordDenied)return <main><h1>임대인 반대 안내</h1><p>특약과 관리규약을 확인하고, 동의를 문자로 남긴 뒤 다시 시도하세요.</p><button onClick={()=>setResult(null)}>다시 시도</button></main>;if(plan)return <main><h1>첫 2주 정착 플랜</h1>{plan.days.map((d:string)=><p>{d}</p>)}<h2>위험 신호</h2>{plan.warnings.map((d:string)=><p>{d}</p>)}</main>;return <main><small>픽스처 모드</small><h1>이름표</h1><p>생활 조건으로 보호소 공고를 함께 검토합니다.</p><label>주거 <select value={p.home} onChange={e=>setP({...p,home:e.target.value})}><option value="STUDIO">원룸</option><option value="APARTMENT">아파트</option><option value="HOUSE">단독주택</option></select></label><label>임대 동의 <select value={p.tenure} onChange={e=>setP({...p,tenure:e.target.value})}><option value="OWN">자가</option><option value="RENT_APPROVED">임대 동의 받음</option><option value="RENT_DENIED">임대인 반대</option></select></label><button onClick={match}>추천 보기</button>{result&&<section><h2>추천 결과</h2>{['GOOD','CONDITIONAL','INFO_LACKING'].map(g=><div><h3>{g}</h3>{result.cards.filter((c:any)=>c.grade===g).map((c:any)=><article><b>{c.animal.raw.kindNm} · {c.animal.kg??'체중 확인'}</b><p>{c.reasons.join(' ')}</p><p>{c.flags.join(' · ')}</p><p>{c.questions.join(' / ')}</p><a href={'tel:'+c.animal.raw.careTel}>{c.animal.raw.careNm} 전화</a></article>)}</div>)}<button onClick={async()=>setPlan((await fetch('/api/v1/settle-plans',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(p)}).then(r=>r.json())).data)}>정착 플랜</button></section>}</main>}createRoot(document.getElementById('root')!).render(<App/>);
