# 이름표(유기견 입양 매칭) Codex 해커톤 준비 계획 (5차, 백엔드 Java Spring, 현장 120분, 사람 주도)

이 문서는 Codex가 실행할 계획입니다. 오늘(2026-09-04) 15:00 시작하는 OpenAI x AI·SW마에스트로 Codex Impact Workshop(2시간 30분 해커톤, Codex 크레딧 100달러, 심사위원은 OpenAI Korea 현직자와 비영리단체 대표)을 위해, 준비 단계에서 만들 것과 현장에서 실행할 순서, 발표 전략을 담습니다.

기준 문서는 `유기견-입양-매칭-설계.md`이고, `국가동물보호정보시스템-API-조사.md`와 `유기견-입양-매칭-판단-기준-아이디어.md`를 참고합니다. 세 문서는 모두 `/Users/hylee/swm/codex-impact-workshop/`에 있습니다.

3차 개정 요지: 백엔드를 TypeScript(Hono)에서 **Java Spring Boot**로 바꿨습니다. 코드 관례는 사용자가 익숙한 `/Users/hylee/sparta/oddventure` 저장소를 따릅니다(단일 모듈, Groovy DSL, 도메인별 계층형 패키지). `~/swm/daesabu/Back-End`의 Modulith·포트 어댑터·스타일 금지 규칙은 따르지 않습니다. 화면(Vite + React)과 보조 스크립트만 Node를 씁니다.

4차·5차 개정 요지: 현장 개발 시간을 **120분**으로 잡고(해커톤 150분 중 마지막 30분은 발표 준비), 백엔드는 **60분** 안에 끝내도록 현장 생성 분량을 절반으로 줄였습니다. Spring 골격(공통 응답·예외, 데이터 타입 record·enum, DTO, 공공 API 클라이언트, AI, 픽스처, 평가 러너, MCP 설정, 지역·헬스 컨트롤러)과 규칙 엔진 테스트 6개를 키트로 들고 가고, Codex는 로직 클래스 13개, 서비스 3개, 컨트롤러 3개, `MatchControllerTest`, `NametagTools`의 21개 파일만 만듭니다. **Goal 같은 자율 루프는 쓰지 않습니다.** 사람이 프롬프트 3개(로직 → 서버 → MCP)를 순서대로 붙여 넣고, 단계 끝마다 사람이 `./gradlew test smoke`와 `node scripts/check.mjs`를 돌려 `docs/CHECKLIST.md`에 체크한 뒤 다음 프롬프트로 넘어갑니다. 훅은 세션 시작 상태 읽기와 편집 후 컴파일 확인 두 개만 둡니다. 40분·60분·70분 확인 지점과 참조 구현 복구 절차를 정했습니다. 설계·API·근거 문서는 키트로 반입하고, 현장에서 만드는 연출은 근거 1건 시연으로 줄였습니다. 팀원이 백엔드 없이 화면을 만들 수 있도록 `scripts/mock-api.mjs`를 추가했습니다. 8절에 시각별로 어느 창에 무엇을 붙여 넣는지 적은 현장 지시 대본을 넣었습니다.

## Context

- 문제: 국가동물보호정보시스템 공고에는 사진과 품종·나이·발견 장소만 있어서 입양 희망자가 자기 생활에 맞는 개를 고르지 못하고, 그 불일치가 행동 문제와 재파양으로 이어집니다.
- 결과물: 질문 9개(1분) → 공공 API 공고를 규칙으로 필터·점수화 → 등급별 추천 카드(추천 이유, 보호소에 물을 질문, 연락처) → 첫 2주 정착 플랜. 규칙은 코드에, LLM은 문장 생성에만 씁니다.
- 이번 계획의 목표 다섯 가지.
  1. Codex가 가장 잘 쓰는 형태의 저장소(AGENTS.md 계층, 스킬, 서브에이전트, 훅, MCP, 플러그인)를 준비합니다.
  2. 현장에서 순서대로 붙여 넣기만 하면 되는 프롬프트와 타임라인을 준비합니다.
  3. 발표에서 어필할 포인트, Q&A 방어 카드, 데모 시나리오를 준비합니다.
  4. 사소한 구성 요소도 Codex 기능으로 감싸서 "Agentic AI 시대에 맞춘 구조"로 보이게 하는 전략을 세웁니다. 기존 md 문서 세 개는 `docs/`로 옮겨 키트에 넣고, 현장에서는 근거 1건을 서브에이전트에 위임하는 장면만 연출합니다.
  5. 완료 정의를 테스트 6개와 체크리스트로 먼저 적어 두고, 사람이 단계마다 검증하며 Codex를 운전하는 모습을 스코어보드와 체크된 체크리스트로 보여 줍니다.

## 확인된 환경 (2026-09-04 조사)

| 항목 | 값 |
| --- | --- |
| Codex CLI | 0.148.0, 기본 모델 `gpt-5.6-sol`, `model_reasoning_effort = "xhigh"`, `service_tier = "fast"`, `plan_mode_reasoning_effort = "max"` |
| 사용 가능 기능(stable, 켜짐) | `goals`, `hooks`, `multi_agent`, `plugins`, `plugin_sharing`, `apps`, `browser_use`, `browser_use_external`, `browser_use_full_cdp_access`, `computer_use`, `in_app_browser`, `image_generation`, `fast_mode`, `guardian_approval`, `skill_search`, `tool_suggest`, `workspace_dependencies` |
| 스킬 경로 | 저장소 `.agents/skills/<name>/SKILL.md`, 개인 `~/.agents/skills/`. 명시 호출은 `$skill-name`, 목록은 `/skills`. `.codex/skills`는 인식되지 않음 |
| 서브에이전트 | `.codex/agents/<name>.toml` (필수 필드 `name`, `description`, `developer_instructions`, 선택 `model`, `model_reasoning_effort`, `sandbox_mode`). 내장 `default`, `worker`, `explorer` |
| 훅 | `.codex/hooks.json` (이벤트 `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, `SubagentStart`, `SubagentStop` 등). stdin으로 JSON, stdout으로 `{"decision":"block","reason":...}` 또는 `hookSpecificOutput.additionalContext` |
| MCP | `codex mcp add <name> -- <cmd>` 또는 프로젝트 `.codex/config.toml`의 `[mcp_servers.<name>]` (신뢰된 프로젝트만). `~/swm`은 이미 trusted |
| 플러그인 | `plugins/<name>/.codex-plugin/plugin.json` + `.agents/plugins/marketplace.json`. 시스템 스킬 `plugin-creator`, `skill-creator`, `skill-installer`, `imagegen`, `openai-docs`, `review-agent` 존재 |
| 설치된 플러그인 | `presentations`, `documents`, `pdf`, `spreadsheets`, `visualize`, `browser`, `ralph-loop` |
| 비대화형 | `codex exec`, `codex review --uncommitted`, `codex cloud exec`(실험) |
| Goals | `/goal <objective>`로 설정, `/goal`로 상태 확인, `/goal pause`, `/goal resume`, `/goal clear`. 스레드 단위로 지속되고 4,000자 이내, idle일 때만 자동 계속. **이 계획에서는 쓰지 않습니다(사용자 결정: 테스트와 체크리스트는 사람이 돌림).** Q&A 카드에 "왜 안 썼나" 답변을 둡니다 |
| Best-of-N | 로컬 CLI에는 없음. `codex cloud exec --attempts N`(1~4, 클라우드 전용, `--help`로 확인). 로컬 대체는 서브에이전트 3개 병렬 + 감사 에이전트 선택 |
| WebMCP | Chrome 152 설치됨. `chrome://flags/#enable-webmcp-testing`을 켜면 `document.modelContext.registerTool(...)`(Chrome 150부터 `navigator.modelContext`는 폐기 예고)과 선언형 폼 속성 `toolname`, `tooldescription`, `toolparamdescription`, `toolautosubmit`을 쓸 수 있음. 테스트는 Chrome의 Model Context Tool Inspector 확장. OpenAI가 2026-08-25~09-03에 WebMCP Challenge를 열었음(파트너 Google Chrome, Cloudflare, Shopify, Vercel, Render, Netlify) |
| Java | JDK 26.0.2와 24.0.2가 설치되어 있고, Gradle 툴체인 캐시(`~/.gradle/jdks`)에 17·21·25가 있음. 시스템 Gradle은 없고 래퍼를 씀. `JAVA_HOME` 미설정 |
| 참조 관례 저장소 | `/Users/hylee/sparta/oddventure`: Spring Boot 3.5.6, Java 툴체인 21(이 프로젝트는 17로 낮춤), Gradle 8.14.3, Groovy DSL `build.gradle`, `settings.gradle`에 foojay 리졸버, Spring AI BOM 1.0.3, 단일 모듈, 루트 패키지 아래 `common/`과 `domain/<기능>/{controller,service,repository,entity,dto/{request,response},exception,enums}` |
| Spring AI | 1.0.x·1.1.x(최신 1.1.8)는 Spring Boot 3.4·3.5와 짝, 2.0.x는 Boot 4.0·4.1과 짝. MCP stdio 스타터는 `org.springframework.ai:spring-ai-starter-mcp-server`. stdio 실행 시 `spring.ai.mcp.server.stdio=true`, `spring.main.web-application-type=none`, `spring.main.banner-mode=off`, `logging.pattern.console=` 빈 값(stdout 오염 금지). 도구는 `@Tool`(`org.springframework.ai.tool.annotation.Tool`) 메서드를 `MethodToolCallbackProvider.builder().toolObjects(bean).build()`로 등록. 리소스는 `McpServerFeatures.SyncResourceSpecification` 빈 목록 |
| 로컬 도구 | Node 26.7, npm 11.19, Python 3.14, uv, gh 2.96 |
| 미확인 | 공공데이터포털 인증키(셸 설정에 없음), OpenAI API 키. 아래 가정 참고 |

## 가정과 결정

- **스택**: 백엔드는 Spring Boot 3.5.6, **Java 17**, Gradle 8.14.3 래퍼, Groovy DSL, **단일 모듈** 하나에 규칙 엔진·REST API·MCP 서버를 모두 담습니다(oddventure와 동일한 형태). MCP 서버는 같은 jar를 `mcp` 프로파일로 띄우는 방식입니다. 화면은 `web/`에 Vite + React를 두고 Vite 프록시(`/api` → `localhost:8080`)로 붙입니다. 훅과 보조 스크립트(`scripts/*.mjs`, `.codex/hooks/*.mjs`)는 Node로 둡니다. "같은 엔진을 사람(웹)도 에이전트(MCP)도 쓴다"는 서사는 그대로입니다. Java 17 문법 범위만 씁니다. record, text block, sealed, `instanceof` 패턴은 쓸 수 있고, switch 패턴 매칭·record 패턴·가상 스레드처럼 21에서 들어온 기능은 쓰지 않습니다.
- **DB 없음**: 저장할 상태가 없으므로 JPA·MySQL·Redis를 넣지 않습니다. 공고는 API 응답 또는 픽스처 JSON을 메모리 캐시에 둡니다. oddventure의 `repository`·`entity` 패키지는 이 프로젝트에는 만들지 않습니다.
- **관례**: 패키지·Lombok·DTO·응답 래퍼·예외·테스트 스타일은 oddventure를 따릅니다(3-1의 `spring-convention` 스킬에 정리). 루트 패키지는 `com.nametag`(oddventure의 `org.example`은 템플릿 기본값이라 바꿉니다). REST Docs·Swagger 파이프라인은 시간이 남을 때만 붙입니다.
- **인증키**: 공공데이터포털 키가 있으면 실데이터로 픽스처를 만들고, 없으면 문서의 실제 값 형식("0,25(Kg)", "2025(60일미만)(년생)", 특징란 문구)으로 합성 픽스처를 만듭니다. 두 경우 모두 `NAMETAG_USE_FIXTURE=1`이면 API 없이 동작해야 합니다. 키 발급은 준비 단계 첫 항목입니다.
- **OpenAI API 키**: LLM 호출은 `ReasonWriter` 인터페이스 뒤에 두고, 키가 없으면 규칙표의 이유 문장 템플릿으로 대체합니다. 호출은 Spring AI 모델 스타터 대신 `RestClient`로 Responses API를 직접 부릅니다(키가 없을 때 자동 설정이 기동을 막는 일을 피함). 데모는 키 없이도 완주해야 합니다.
- **준비 범위**: 저장소 전체(키트 + 참조 구현 + 문서 + 프롬프트)를 `/Users/hylee/swm/codex-impact-workshop/nametag/`에 만들고 리허설까지 합니다. 현장에는 **키트**를 반입합니다. 키트에는 설정·문서·스킬 외에 Spring **골격**(산출물 구조에 [키트]로 표시한 파일: `NametagApplication`, `common/**`, 데이터 타입 record·enum, DTO 전부, `AnimalCatalog` 두 구현, `ReasonWriter` 세 파일, `McpResourceConfig`, `FixtureLoader`, `EvaluateRunner`, 지역·헬스 컨트롤러, 도메인별 `ErrorCode`·`Exception`)과 **규칙 엔진 테스트 6개**가 들어 있습니다. Codex는 현장에서 로직 클래스 13개, 서비스 3개, 컨트롤러 3개, `MatchControllerTest`, `NametagTools`의 21개 파일과 `web/src`를 만듭니다. 골격이 참조하는 클래스가 아직 없으므로 키트는 컴파일이 깨진 상태로 시작하며, 컴파일 오류와 테스트가 곧 Codex에게 주는 사양입니다. 막히면 참조 구현에서 파일 단위로 복구합니다.
- **규칙 기준**: 설계 문서의 하드 필터 5개와 점수표를 그대로 씁니다. 아이디어 문서에서는 구조만 채택합니다. 상쇄되면 안 되는 조합 4개를 확인 플래그(비보상형)로 분리하고, 순위 대신 "잘 맞음 / 조건부(확인 n개) / 정보 부족" 3등급으로 보여 주며, 특징란이 빈 개를 "정보 부족" 등급으로 노출합니다. 점수 가중치 조정은 현장 여유 시간용 후보로만 둡니다.
- **서비스 이름**: 이름표. 저장소 슬러그 `nametag`.
- **문구 원칙**: 출처가 확인되지 않은 수치(예: "78%")는 쓰지 않습니다. Powell 2023 기준 "입양자 3분의 2가 문제행동을 경험"으로 씁니다.
- **진행 방식(사람 주도)**: Goal이나 Stop 훅 같은 자율 루프는 쓰지 않습니다. 사람이 `prompts/03-core.md`(로직 13개), `prompts/04-server.md`(서비스·컨트롤러), `prompts/06-mcp.md`(`NametagTools`)를 순서대로 창 A에 붙여 넣습니다. 각 프롬프트는 한 단계만 하고, 끝나면 변경 파일과 실행 결과를 보고하고 멈추도록 씁니다. 단계 끝마다 사람이 `./gradlew -q test smoke --offline`과 `node scripts/check.mjs --quick`(마지막 단계는 전체)을 직접 돌리고 `docs/CHECKLIST.md`의 항목에 체크한 뒤 커밋하고 다음 프롬프트로 넘어갑니다. Codex 스레드는 `-c model_reasoning_effort="high"`로 시작합니다.
- **현장 120분과 확인 지점**: 해커톤 150분 중 발표 준비 30분을 빼면 개발은 120분이고, 그중 백엔드 프롬프트 3개에 60분을 씁니다. 40분(로직), 60분(서버), 70분(MCP)에 체크리스트가 통과하지 않으면 고민하지 않고 참조 구현에서 빠진 파일만 복구합니다. 70분 이후에는 새 코드를 시작하지 않고 데모·통합·리허설로 갑니다. 00-kickoff 플랜 모드, 근거표 8건 팬아웃, Best-of-N, WebMCP는 현장 주 경로에서 빼고 선택 사항으로 둡니다.
- **평가 도구**: `domain/evaluate`의 `Evaluator`가 근거 없는 문장 0건, 보호 종료 개체 추천 0건, 하드 필터 위반 0건, 잘 맞음 등급의 확인 플래그 0건, 특징란 커버리지를 지표로 냅니다. 이 지표를 체크리스트 성공 조건, MCP 도구 `evaluate_match`, REST `GET /api/v1/evaluations`가 함께 씁니다.
- **목 API 서버**: `scripts/mock-api.mjs`(의존성 없는 Node http 서버, 8080 포트)가 `fixtures/`의 JSON을 `ApiResponse` 형식으로 응답합니다. 팀원은 백엔드가 나오기 전부터 이 서버로 화면 4개를 만들고, 백엔드가 실패했을 때는 데모 백업이 됩니다.
- **WebMCP**: 웹 화면의 질문 폼과 추천 함수를 WebMCP 도구로도 노출합니다. API가 없는 브라우저에서는 조용히 건너뜁니다. 데모는 Chrome 플래그를 켠 노트북 1대에서만 합니다.
- **하지 않을 것**: 스킬을 개수로 늘리지 않습니다(9개 고정). MCP 도구는 5개, 서브에이전트는 4개까지입니다. 발표에서 동작 화면으로 보여 줄 수 없는 항목은 계획에 넣지 않습니다. Goal, Stop 훅, 자동 재시도 같은 자율 루프는 넣지 않습니다. 테스트 실행과 체크리스트 확인은 사람이 합니다.

## 산출물 구조

```text
nametag/
├── AGENTS.md                      # 루트 규칙 (10줄 이내 핵심 규칙 + 작업 라우팅 + 완료 정의)
├── build.gradle                   # Groovy DSL, Boot 3.5.6, Java 17 toolchain, Lombok, Spring AI MCP, jacoco
├── settings.gradle                # foojay-resolver-convention 1.0.0, rootProject.name = 'nametag'
├── gradlew, gradlew.bat, gradle/wrapper/   # Gradle 8.14.3
├── .env.example                   # DATA_GO_KR_SERVICE_KEY, OPENAI_API_KEY, NAMETAG_USE_FIXTURE
├── src/
│   ├── AGENTS.md                  # Java 하위 규칙 (spring-convention 스킬 요약, 규칙은 Rules.ALL에만, 골격·테스트 수정 금지)
│   ├── main/java/com/nametag/       # [키트] = 반입, [현장] = Codex가 프롬프트 3개로 생성 (21개)
│   │   ├── NametagApplication.java                                              [키트]
│   │   ├── common/{config, dto/response, exception}                              [키트]
│   │   ├── domain/animal/model/{RawAnimal, Animal, NeedLevel, Keyword, InfoLevel}   [키트] 데이터 타입
│   │   ├── domain/animal/model/{AnimalParser, AnimalDeriver}                        [현장]
│   │   ├── domain/animal/{client, controller, service, dto/response, exception}     [키트] 카탈로그·지역·헬스
│   │   ├── domain/match/model/{Profile, Rule, EvidenceGrade, Grade, ScoreResult, MatchOutput, MatchCard}   [키트] 데이터 타입
│   │   ├── domain/match/model/{HardFilters, CheckFlags, Rules, Scorer, Grader, ShelterQuestions, ReasonTemplates, ReasonValidator, MatchEngine}   [현장]
│   │   ├── domain/match/{dto/{request,response}, exception}   [키트]    domain/match/{controller, service}   [현장]
│   │   ├── domain/settle/{model/SettlePlan, dto}   [키트]    domain/settle/{model/SettlePlanBuilder, controller, service}   [현장]
│   │   ├── domain/evaluate/{model/Evaluation, dto}   [키트]    domain/evaluate/{model/Evaluator, controller, service}   [현장]
│   │   ├── domain/ai/{ReasonWriter, TemplateReasonWriter, OpenAiReasonWriter, config}   [키트]
│   │   ├── domain/mcp/config/McpResourceConfig   [키트]    domain/mcp/tools/NametagTools   [현장]
│   │   └── support/{fixture/FixtureLoader, evaluate/EvaluateRunner}   [키트]
│   ├── main/resources/{application.yml, application-mcp.yml, application-evaluate.yml, rules/rules.md, evidence/evidence.md}   [키트]
│   └── test/java/com/nametag/       # 엔진 테스트 6개 [키트], MatchControllerTest [현장]
├── web/                           # Vite + React (AGENTS.md, src/webmcp.ts 포함)
├── .agents/
│   ├── skills/                    # 저장소 스킬 9개 (현장 반입 대상)
│   │   ├── spring-convention/     # oddventure 관례
│   │   ├── animal-api/
│   │   ├── matching-rules/
│   │   ├── settle-plan/
│   │   ├── persona-smoke/
│   │   ├── evidence-cite/
│   │   ├── demo-pitch/
│   │   ├── ui-smoke/
│   │   └── ship/
│   └── plugins/marketplace.json   # 로컬 마켓플레이스 (plugins/nametag 등록)
├── .codex/
│   ├── config.toml                # [agents]만. MCP 블록은 jar가 생긴 뒤(70분) 아래 파일을 이어 붙임
│   ├── mcp.toml.part              # [mcp_servers.nametag] (java -jar ... --spring.profiles.active=mcp)
│   ├── hooks.json                 # SessionStart, PostToolUse(컴파일)만. Stop 훅 없음
│   ├── hooks/                     # session-start.mjs, post-edit-compile.mjs
│   └── agents/                    # evidence-researcher.toml, rule-auditor.toml, persona-tester.toml, ui-builder.toml
├── plugins/nametag/               # 플러그인 패키지 (.codex-plugin/plugin.json, skills/, .mcp.json, hooks/)
├── fixtures/                      # animals.json, animals.sample.json, sido.json, sigungu.json, personas.json, coverage.json,
│                                  # match.request.json, match.sample.json, settle-plan.sample.json (목 API와 curl 확인용)
├── docs/                          # CHECKLIST.md, design.md, evidence.md, api.md, pitch.md, qa.md, codex-playbook.md, timeline.md, screens/
├── prompts/                       # 주 경로 10개(00-setup ~ 09-ship) + 선택 3개 (4-2 참고)
└── scripts/                       # export-kit.sh, health.mjs, fetch-all.mjs, coverage.mjs, build-plugin.mjs, check.mjs, ui-smoke.mjs, mock-api.mjs
```

## Phase 0. 사전 확인 (사람이 할 일, 15분)

1. 공공데이터포털에서 [구조동물 조회 서비스](https://www.data.go.kr/data/15098931/openapi.do) 활용신청(자동승인) 후 인증키를 `.env`의 `DATA_GO_KR_SERVICE_KEY`에 넣습니다. PC 웹에서만 신청됩니다.
2. `scripts/health.mjs`(Phase 1에서 생성)로 `sido_v2`를 호출해 반영 여부를 확인합니다. 키가 게이트웨이에 반영되지 않았으면 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`가 나오므로 픽스처 모드로 진행합니다.
3. OpenAI API 키가 있으면 `OPENAI_API_KEY`에 넣습니다. 없으면 비워 둡니다.
4. 두 노트북 모두에서 `./gradlew build`를 한 번 돌려 Gradle 8.14.3, JDK 17(foojay가 자동 내려받음. 이 노트북의 Gradle 캐시에는 이미 있음), 의존성을 캐시에 채워 둡니다. 현장 네트워크가 나빠도 `--offline`으로 빌드되게 하려는 것입니다. `web/`에서도 `npm install`을 한 번 돌려 npm 캐시를 채웁니다(현장에서는 `npm install --prefer-offline`).
5. 현장에서 쓸 GitHub 저장소 이름을 정해 둡니다(예: `nametag`). 두 노트북이 코드를 주고받는 통로이므로 필수입니다. `gh auth status`로 두 노트북 모두 로그인되어 있는지 확인합니다.
6. 참조 구현 경로를 확인해 둡니다(`/Users/hylee/swm/codex-impact-workshop/nametag/`). 8절의 복구 명령은 이 경로를 `REF`로 부릅니다. 현장 폴더는 `~/hack/nametag`입니다.
7. 팀원 노트북에도 Codex CLI 0.148 이상과 `browser`·`presentations` 플러그인이 설치되어 있는지 확인합니다(`$ui-smoke`와 `$demo-pitch`가 씁니다).
8. 발표 노트북의 Chrome 152에서 `chrome://flags/#enable-webmcp-testing`을 미리 켜고 Model Context Tool Inspector 확장을 설치해 둡니다(WebMCP를 붙일 때만 필요).

## Phase 1. Gradle 프로젝트와 규칙 엔진 (Codex, 약 50분)

### 1-1. Gradle 프로젝트

- `build.gradle`(Groovy DSL): 플러그인 `java`, `org.springframework.boot 3.5.6`, `io.spring.dependency-management 1.1.7`, `jacoco`. `group = 'com.nametag'`, `version = '0.0.1-SNAPSHOT'`. `java { toolchain { languageVersion = JavaLanguageVersion.of(17) } }`. `configurations { compileOnly { extendsFrom annotationProcessor } }`.
- 의존성(oddventure 방식으로 `// spring`, `// lombok`, `// ai`, `// test` 주석 그룹): `spring-boot-starter-web`, `spring-boot-starter-validation`, Lombok(`compileOnly` + `annotationProcessor`, 테스트 쪽도 같은 두 줄), `org.springframework.ai:spring-ai-starter-mcp-server`(BOM `spring-ai-bom:1.0.3`을 `dependencyManagement { imports { mavenBom ... } }`로), `spring-boot-starter-test`, `junit-platform-launcher`(testRuntimeOnly).
- 테스트 태스크: `test`는 `useJUnitPlatform { excludeTags 'smoke' }`, 별도 `tasks.register('smoke', Test)`는 `includeTags 'smoke'`. `test`는 `finalizedBy jacocoTestReport`, `jacocoTestCoverageVerification`의 최소치 0.40(oddventure와 동일), `check.dependsOn jacocoTestCoverageVerification`.
- `settings.gradle`: `plugins { id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0' }`, `rootProject.name = 'nametag'`.
- `src/main/resources/application.yml`: `spring.application.name: nametag`, `spring.config.import: optional:file:.env[.properties]`, `nametag.fixture: ${NAMETAG_USE_FIXTURE:false}`, `nametag.data-go-kr.base-url: https://apis.data.go.kr/1543061/abandonmentPublicService_v2/`, `nametag.data-go-kr.service-key: ${DATA_GO_KR_SERVICE_KEY:}`, `nametag.openai.api-key: ${OPENAI_API_KEY:}`, `spring.http.client.connect-timeout: 3s`, `read-timeout: 5s`. 포트는 기본 8080.
- `application-mcp.yml`: `spring.main.web-application-type: none`, `spring.main.banner-mode: off`, `logging.pattern.console: ""`, `logging.file.name: build/mcp.log`, `spring.ai.mcp.server.stdio: true`, `spring.ai.mcp.server.name: nametag`, `spring.ai.mcp.server.version: 0.1.0`, `spring.ai.mcp.server.type: SYNC`.
- `application-evaluate.yml`: web 없음, 배너 끔, 콘솔 로깅 끔. `EvaluateRunner`(`CommandLineRunner`, `@Profile("evaluate")`)가 지표 JSON 한 줄을 stdout에 쓰고 종료합니다.
- `web/`: `npm create vite@latest web -- --template react-ts`, `vite.config.ts`에 `server.proxy['/api'] = 'http://localhost:8080'`. 루트에는 `package.json`을 두지 않고 `scripts/*.mjs`는 의존성 없는 순수 Node로 씁니다.

### 1-2. 규칙 엔진 (`com.nametag.domain.animal.model`, `com.nametag.domain.match.model`)

Spring 의존이 없는 순수 Java 클래스입니다. record와 정적 메서드를 기본으로 하고, Lombok은 `@Getter`·`@Builder`·`@RequiredArgsConstructor`만 씁니다. 표의 클래스 중 데이터 타입(`RawAnimal`, `Animal`, `NeedLevel`, `Keyword`, `InfoLevel`, `Profile`, `Rule`, `EvidenceGrade`, `Grade`, `ScoreResult`, `MatchOutput`, `MatchCard`, `SettlePlan`, `Evaluation`)은 키트로 반입하고, 로직 클래스 13개(`AnimalParser`, `AnimalDeriver`, `HardFilters`, `CheckFlags`, `Rules`, `Scorer`, `Grader`, `ShelterQuestions`, `ReasonTemplates`, `ReasonValidator`, `MatchEngine`, `SettlePlanBuilder`, `Evaluator`)는 현장에서 Codex가 만듭니다. 로직 클래스의 시그니처는 `docs/CHECKLIST.md`의 클래스 표와 키트 테스트가 참조하는 것과 같아야 합니다.

| 클래스 | 내용 |
| --- | --- |
| `animal.model.RawAnimal` | API 필드 그대로의 record(`desertionNo`, `kindNm`, `age`, `weight`, `specialMark`, `processState`, `noticeEdt`, `careNm`, `careTel`, `careAddr`, `popfile1`, `neuterYn`, `sexCd`, `orgNm`). `@JsonIgnoreProperties(ignoreUnknown = true)` |
| `animal.model.AnimalParser` | `static Double parseKg(String)`: 공백 제거 → 정규식 `^([\d.,]+)\(?Kg\)?` (대소문자 무시) → 콤마를 점으로 → 0·결측·불일치는 `null`. `static Birth parseBirth(String)`: 앞 4자리 연도, "60일미만" 포함 여부로 `neonate`. `record Birth(Integer year, boolean neonate)` |
| `animal.model.Animal` | 파생 변수를 담은 record. `raw`, `kg`(nullable), `birthYear`, `neonate`, `ageApprox`(`2026 - birthYear`), `adultSizeUnknown`(체중 없음 또는 60일미만 또는 1세 이하 믹스견), `needLevel`(enum `NeedLevel` LOW/MID/HIGH), `keywords`(`Set<Keyword>`), `dangerousBreed`, `infoLevel`(enum `InfoLevel`), `photoUrl`(`http://` → `https://`) |
| `animal.model.AnimalDeriver` | `static Animal derive(RawAnimal, int currentYear)`. 키워드는 긍정 그룹을 먼저 평가해 "사람경계 없음"이 주의로 잡히지 않게 함. `needLevel`은 낮음(8세 이상 또는 얌전·조용·노령) / 높음(1~3세이고 10kg 이상 또는 활발·에너지) / 나머지 중간. 맹견은 도사·핏불·스태퍼드셔·로트와일러 부분 일치. `infoLevel`은 특징란이 비었거나 신체 상태만이면 `LACKING` |
| `match.model.Profile` | 질문 9개 답을 담는 record. 필드명과 enum은 설계 문서 질문 표를 그대로 옮기고 `docs/design.md`에 표로 남김 |
| `match.model.HardFilters` | 하드 필터 5개: 맹견, 크기 제한(`kg >= 상한`이면 제외, `adultSizeUnknown`이면 제외하지 않고 배지), 지역(API 파라미터이므로 통과), 임대인 반대(`MatchEngine`이 결과 대신 `landlordDenied` 상태 반환), 만 7세 미만 아이 + 입질·사나움·공격 기록 |
| `match.model.CheckFlags` | 확인 플래그 4개(점수로 상쇄되지 않음): 다른 개 싫어함 + 다른 반려동물, 분리불안 기록 + 8시간 이상 부재, 겁많음 기록 + 아이, 기대치 "며칠" + 문제행동 기록 |
| `match.model.Rule`, `Rules` | `record Rule(String id, BiPredicate<Profile, Animal> when, int delta, String reason, EvidenceGrade grade)`. `Rules.ALL`은 설계 문서 점수표를 옮긴 `List<Rule>`. `kg`가 null이면 체중 조건은 가산도 감점도 없음. 활동 등급 매핑: 0~1일 낮음, 2~3일 중간, 4~7일 높음 |
| `match.model.Scorer` | `ScoreResult score(Profile, Animal)`: 합계와 적용된 규칙 id 목록 |
| `match.model.Grader` | enum `Grade` GOOD(플래그 0, 점수 ≥ 0, 정보 있음) / CONDITIONAL(플래그 ≥ 1 또는 점수 < 0) / INFO_LACKING. 등급 안에서는 점수 내림차순, 동점은 `noticeEdt` 임박 순 |
| `match.model.ShelterQuestions` | 체중 미기재 → "성견 체중 예상은?", 특징란 비었음 → "사람과 다른 개에게 어떤가?", 60일미만 → "부모견 크기는?", 항상 → "임시보호 경험이 있나?" |
| `match.model.ReasonTemplates`, `ReasonValidator` | 규칙별 이유 문장 템플릿(LLM 없을 때 그대로 출력). `ReasonValidator.validate(List<String> sentences, List<String> appliedRuleIds)`: 적용된 규칙 목록 밖의 주장이 섞인 문장을 버림 |
| `match.model.MatchEngine` | `MatchOutput match(Profile, List<Animal>)`: 필터 → 플래그 → 점수 → 등급 → 질문. 임대인 반대면 `MatchOutput.landlordDenied()` |
| `settle.model.SettlePlanBuilder` | 2주 정착 플랜: 첫 줄은 8번 답(며칠/몇 주/몇 달)에 따라 달라짐, 주거(공동주택 인사·엘리베이터 안고 타기, 임대 동의 문자, 제한 모르면 관리사무소 확인), 10시간 초과 부재 시 펫시터, 크기별 월 비용 안내, 14일차 3문항 설문 |
| `evaluate.model.Evaluator` | `Evaluation evaluate(List<Profile> personas, List<RawAnimal>)`: 근거 없는 문장 건수, 보호 종료(`processState`가 보호중이 아님) 추천 건수, 하드 필터 위반 건수, 잘 맞음 등급에 섞인 확인 플래그 건수, 특징란 커버리지, 등급 분포, 위반 목록 |

테스트는 JUnit 5 + AssertJ + Mockito, oddventure 스타일(`@DisplayName` 한국어 문장, 메서드명 `기능_success` / `기능_fail_사유`, `// given` `// when` `// then` 주석)로 설계 문서 "검증할 것"을 그대로 옮깁니다. 아래 테스트 6개는 모두 키트에 들어가고, 현장에서 Codex는 수정하지 않고 통과시킵니다. 테스트가 곧 사양입니다.

- `AnimalParserTest`: `@ParameterizedTest` + `@MethodSource`로 "0,25(Kg)" → 0.25, "11(Kg)" → 11, " 3,50 (Kg)" → 3.5, "" → null, "0(Kg)" → null. `parseBirth`: "2025(60일미만)(년생)" → {2025, true}, "2022(년생)" → {2022, false}
- `HardFiltersTest`: "핏불테리어", "도사 믹스" 제외. 만 7세 미만 아이 + 입질 제외
- `AnimalDeriverTest`: "사람경계 없음"이 주의 키워드로 잡히지 않음
- `MatchEngineTest`: 활동량 +2가 있어도 "다른 개 싫어함 + 다른 반려동물"은 조건부 등급
- `ReasonValidatorTest`: 규칙 목록 밖 문장은 제거됨
- `PersonaSmokeTest`(`@Tag("smoke")`): `fixtures/personas.json` 20명 × 픽스처 공고 전체. 불변식은 3-2 `persona-smoke` 항목 참고. 끝에 `Evaluator` 결과를 `build/evaluation.json`에 씁니다. `check.mjs --quick`이 이 파일로 지표를 읽습니다

### 1-3. `fixtures/`

- 키가 있으면 `scripts/fetch-all.mjs`로 `state=protect`, `numOfRows=1000`, 3페이지를 받아 `fixtures/animals.json`(전량)과 `fixtures/animals.sample.json`(60건, 변형 골고루)을 만듭니다. `scripts/coverage.mjs`로 특징란에 성격 키워드가 하나라도 있는 비율을 재서 `fixtures/coverage.json`에 저장합니다(아이디어 문서 4-2 스파이크. 발표에서 먼저 말할 숫자).
- 키가 없으면 60건을 합성합니다. 반드시 포함할 변형: 콤마 소수점 체중, 체중 0, 체중 결측, 60일미만, 8세 이상, 맹견 표기 2종, 특징란 공란, "사람경계 없음", "겁이많고 사람을 견제함", 짖음, 분리불안, 다른 개 싫어함, http 사진 URL.
- `fixtures/sido.json`, `fixtures/sigungu.json`(시도 2~3개분), `fixtures/personas.json`(입양자 20명: 원룸 초보, 아파트 아이 있음, 임대인 반대, 크기 제한 10kg, 10시간 초과 부재, 65세 이상 동거, 다른 개 있음, 단독주택 경험자 등).
- `fixtures/match.request.json`(`POST /api/v1/matches` 요청 본문 예시 1개), `fixtures/match.sample.json`, `fixtures/settle-plan.sample.json`(참조 구현이 낸 실제 응답을 저장한 파일). `scripts/mock-api.mjs`가 `GET /api/v1/health`, `GET /api/v1/regions/sido`, `GET /api/v1/regions/sigungu`, `POST /api/v1/matches`, `POST /api/v1/settle-plans`를 이 파일들로 응답합니다. 실행은 `node scripts/mock-api.mjs`(8080 포트, 의존성 없음)입니다. 팀원의 화면 작업과 데모 백업에 씁니다.
- `support/fixture/FixtureLoader`가 `fixtures/` 경로(설정 `nametag.fixture-dir`, 기본 `./fixtures`)에서 Jackson으로 읽습니다.

## Phase 2. API, 화면, MCP (Codex, 약 60분)

### 2-1. REST API (Spring MVC, oddventure 관례)

- 키트 반입과 현장 생성의 경계: 키트에는 `ApiResponse`와 예외 공통, DTO 전부(`fixtures/match.sample.json`과 같은 형태. 화면과 목 API가 같은 계약을 씀), `AnimalCatalog` 두 구현, `ReasonWriter` 세 파일, `HealthController`·`RegionController`·`AnimalService`, 도메인별 `ErrorCode`·`Exception`이 들어갑니다. 현장에서 Codex가 만드는 것은 `MatchService`, `SettleService`, `EvaluateService`, `MatchController`, `SettlePlanController`, `EvaluationController`, `MatchControllerTest`입니다.
- 공통: `common/dto/response/ApiResponse<T>`(필드 `httpStatus`, `code`, `success`, `message`, `data`, `timestamp`, 정적 팩토리 `success(data, message)`·`created`·`noContent`가 `ResponseEntity<ApiResponse<T>>` 반환), `common/exception/{ErrorCode(interface), CommonErrorCode, GlobalException, GlobalExceptionHandler(@RestControllerAdvice)}`. 도메인마다 `XxxErrorCode implements ErrorCode` enum + `XxxException extends GlobalException`. 메시지는 한국어.
- 컨트롤러는 `@RestController @RequestMapping("/api/v1/<복수형>") @RequiredArgsConstructor`, 요청은 `@Valid @RequestBody` record, 반환은 항상 `ResponseEntity<ApiResponse<...>>`. 서비스는 `@Slf4j @Service @RequiredArgsConstructor`.
- 엔드포인트:
  - `GET /api/v1/health`: 키 존재 여부(마스킹), 픽스처 모드 여부, 공고 캐시 건수.
  - `GET /api/v1/regions/sido`, `GET /api/v1/regions/sigungu?uprCd=`: `sido_v2`, `sigungu_v2` 프록시. 픽스처 모드면 파일 응답.
  - `POST /api/v1/matches`: `MatchRequest`(Profile + 지역) → `AnimalCatalog.search(uprCd, orgCd)`(`abandonmentPublic_v2`, `upkind=417000`, `state=protect`, 10분 캐시) → `MatchEngine.match` → 이유 문장(`ReasonWriter`) → `ReasonValidator` → `MatchResponse`(등급별 카드 목록, 배지, 질문, 보호소 연락처, 마감일).
  - `POST /api/v1/settle-plans`: `SettlePlanBuilder` → LLM이 있으면 문장 다듬기, 없으면 템플릿.
  - `GET /api/v1/evaluations`: `Evaluator` 결과. `scripts/check.mjs`와 MCP `evaluate_match`가 같은 서비스를 씁니다.
- `domain/animal/client`: `AnimalCatalog` 인터페이스, `DataGoKrAnimalCatalog`(`RestClient.Builder` 주입, `@ConditionalOnProperty(name = "nametag.fixture", havingValue = "false", matchIfMissing = true)`), `FixtureAnimalCatalog`(`havingValue = "true"`). 공공 API base URL은 반드시 v2. 구버전 `abandonmentPublicSrvc`는 폐기됨.
- `domain/ai`: `ReasonWriter` 인터페이스, `TemplateReasonWriter`(기본), `OpenAiReasonWriter`(`nametag.openai.api-key`가 비어 있지 않을 때만 빈 등록, Responses API를 `RestClient`로 호출, 모델은 실행 시점에 `$openai-docs` 스킬로 가장 저렴한 현행 소형 모델을 확인해 설정값으로 둠, 입력은 사람 입력·개 필드·적용 규칙 목록만).
- 컨트롤러 테스트는 `@WebMvcTest(MatchController.class)` + `@MockitoBean MatchService`(Boot 3.4 이상이므로 `@MockBean` 대신 `@MockitoBean`), `jsonPath("$.success")`와 `$.data` 검증.

### 2-2. `web/` (Vite + React)

화면 4개. 스타일은 최소(Tailwind 또는 CSS 변수), 현장에서 팀원이 다듬습니다. 모든 응답은 `ApiResponse` 래퍼이므로 `data`를 풀어서 씁니다.

1. 질문 화면: 설계 문서 표의 9문항, 닫힌 선택지, 지역은 API에서 동적 로드. 1분 안에 끝나는 길이.
2. 결과 화면: 3등급 섹션. 카드에는 사진(https), 이름표 형식의 요약(품종·나이·체중), 배지("성견 체중 확인 필요", "정보 부족", 확인 플래그 노란 배지), 추천 이유 2~3문장, 보호소에 물을 질문, 보호소 이름·전화(`tel:` 링크)·공고 마감일. "품종으로 성격을 단정하지 않습니다" 한 줄.
3. 임대인 반대 안내 화면: 특약 효력, 동의 받는 순서, 다시 시도 버튼.
4. 정착 플랜 화면: 날짜별 할 일, 지켜볼 위험 신호, 보호소 연락처, 14일차 설문 3문항.

상단에 "픽스처 모드" 표시를 두어 데모 중 API 장애에 대비합니다. 백엔드가 아직 없을 때는 `node scripts/mock-api.mjs`가 같은 엔드포인트를 픽스처로 응답하므로, 화면은 처음부터 실제 경로(`/api/v1/...`)로 만들고 목과 실제를 구분하지 않습니다.

### 2-3. MCP 서버 (같은 jar, `mcp` 프로파일)

- `NametagTools`는 현장에서 Codex가 만들고, `McpResourceConfig`와 `application-mcp.yml`은 키트입니다. 도구 5개의 이름·설명·입력 필드는 `docs/CHECKLIST.md`의 표에 미리 적어 둡니다.
- `domain/mcp/tools/NametagTools`(`@Component`)에 `@Tool(description = ...)` 메서드 5개: `searchAnimals(sido, sigungu, limit)`, `match(profileJson, region)`(등급·이유·보호소 질문 포함), `shelterQuestions(desertionNo)`, `settlePlan(profileJson, desertionNo)`, `evaluateMatch(personaIds)`(평가 지표와 위반 목록. "Codex가 자기 결과물을 도구로 다시 평가한다"는 장면용). 도구 이름은 MCP에 `search_animals`, `match`, `shelter_questions`, `settle_plan`, `evaluate_match`로 노출되도록 `@Tool(name = ...)`을 명시합니다.
- `domain/mcp/config/McpResourceConfig`: `ToolCallbackProvider` 빈(`MethodToolCallbackProvider.builder().toolObjects(nametagTools).build()`)과 `List<McpServerFeatures.SyncResourceSpecification>` 빈으로 리소스 2개(`nametag://rules`는 `resources/rules/rules.md`, `nametag://evidence`는 `resources/evidence/evidence.md`).
- 실행: `./gradlew bootJar` 후 `java -jar build/libs/nametag-0.0.1-SNAPSHOT.jar --spring.profiles.active=mcp`. stdout에는 JSON-RPC만 나가야 하므로 `application-mcp.yml`의 배너·콘솔 로깅 설정을 지킵니다.
- `.codex/config.toml`에 등록합니다. 키트의 `config.toml`에는 `[agents] max_concurrent_threads_per_session = 3`만 있고, 아래 블록은 `.codex/mcp.toml.part`에 따로 둡니다. jar가 없는 상태에서 세션을 열면 MCP 서버 기동 실패로 시작이 늦어지기 때문입니다. `bootJar` 뒤에 `cat .codex/mcp.toml.part >> .codex/config.toml`로 이어 붙이고 새 세션을 엽니다(MCP 서버 목록은 세션 시작 때 읽힘).

```toml
[mcp_servers.nametag]
command = "java"
args = ["-jar", "build/libs/nametag-0.0.1-SNAPSHOT.jar", "--spring.profiles.active=mcp"]
startup_timeout_sec = 60
tool_timeout_sec = 60

[mcp_servers.nametag.env]
NAMETAG_USE_FIXTURE = "1"
```

- 데모 문장: Codex 세션에서 "서울 강남구, 원룸, 처음 키우는 사람에게 맞는 개 3마리를 이유와 함께 찾아 줘"라고 하면 Codex가 MCP 도구를 호출해 답합니다. `codex mcp list`에 `nametag`가 enabled로 보여야 합니다. 이어서 "방금 추천을 evaluate_match로 평가해 줘"라고 하면 지표 표가 나옵니다.

### 2-4. WebMCP (`web/src/webmcp.ts`)

같은 매칭 엔진을 웹 페이지 자체가 에이전트에게 도구로 노출합니다. MCP가 "에이전트에게 외부 도구를 주는 것"이라면 WebMCP는 "웹 페이지가 도구가 되는 것"입니다.

- 진입점은 `document.modelContext`이고, 없으면 `navigator.modelContext`, 둘 다 없으면 아무것도 하지 않습니다. 오류를 내지 않습니다.
- `registerTool` 2개: `recommend_dogs({ profile })`는 `/api/v1/matches`를 호출해 등급·이름표 요약·보호소 전화를 돌려주고, `get_dog({ desertionNo })`는 공고 하나를 돌려줍니다. 둘 다 `annotations.readOnlyHint = true`.
- 질문 폼에 선언형 속성을 붙입니다: `<form toolname="match_profile" tooldescription="입양 희망자의 생활 조건 9개를 받아 맞는 개를 추천한다">`, 각 `select`에는 `toolparamdescription`으로 선택지 뜻을 적습니다.
- 데모 절차: Chrome 152에서 `chrome://flags/#enable-webmcp-testing`을 Enabled로 바꾸고 재시작 → Model Context Tool Inspector 확장 설치 → 화면을 연 상태에서 확장에 "원룸에 사는 초보에게 맞는 개 찾아 줘" 입력 → 폼이 채워지고 결과가 나오는 것을 확인합니다. 확장이 안 되면 DevTools 콘솔에서 `await document.modelContext.getTools()`로 도구 2개가 나열되는 화면을 백업 스크린샷으로 둡니다.
- 포장 문장: "사람이 쓰는 웹과 에이전트가 쓰는 웹을 같은 화면에서 제공한다. OpenAI가 지난주까지 WebMCP Challenge를 연 바로 그 규격이다."

## Phase 3. Codex 키트: 스킬, 서브에이전트, 훅, 플러그인, 체크리스트 (Codex, 약 50분)

이 단계가 "별거 아닌 것을 Codex 기술로 감싸는" 핵심입니다. 각 항목은 실제로 동작해야 하고, 발표 문장 하나씩을 가집니다.

### 3-1. 루트 `AGENTS.md`

- 형식은 한 줄 소개, 핵심 규칙 목록, 상세 문서 표, 문서 추가 방법 순서입니다.
- 핵심 규칙(10줄 이내): v2 엔드포인트만 사용, 규칙은 `domain/match/model/Rules.ALL`에만 두고 LLM은 문장만, 체중·나이 파서는 테스트 케이스 유지, 사진 URL https 치환, `NAMETAG_USE_FIXTURE=1`이면 네트워크 금지, Java 코드는 `$spring-convention`을 따름(oddventure 관례), 문서·주석·커밋 한국어, 커밋은 `feat:`·`fix:`·`test:`·`refactor:`·`chore:`·`docs:` 접두어 + 한국어 제목(oddventure와 동일, scope 없음), 출처 없는 수치 금지(근거는 `docs/evidence.md`만), 규칙을 바꾸면 `rule-auditor`에게 감사 위임, 스킬은 `.agents/skills`에만 둠, **한 프롬프트는 지시받은 단계만 하고 끝나면 변경 파일과 실행 결과를 보고하고 멈춤(다음 단계로 스스로 넘어가지 않음, 질문 대신 가정을 적고 진행)**, 작업 끝에 `$ship`.
- 완료 정의(Definition of Done) 절 6줄: 구현이 존재함, `./gradlew test smoke` 통과, `node scripts/check.mjs --quick`이 CHECK 1 PASSED 출력(규칙 엔진), `node scripts/check.mjs`가 ALL CHECKS PASSED 출력(REST와 MCP), 화면 4개를 `$ui-smoke`로 확인, 문서 갱신. 이 여섯 줄은 `docs/CHECKLIST.md`의 성공 조건 8개와 같은 내용이어야 합니다. 검증 명령은 사람이 돌립니다.
- 작업 라우팅 표: Java 작성 → `$spring-convention`, API 작업 → `$animal-api`, 규칙 변경 → `$matching-rules`, 플랜 문구 → `$settle-plan`, 검증 → `$persona-smoke`, 발표 → `$demo-pitch`.
- 하위 `src/AGENTS.md`(순수 Java 규칙 엔진은 Spring 의존 금지, 규칙은 `Rules.ALL`에만 추가, 키트 골격과 `src/test/java`의 테스트 6개는 수정 금지이며 그것들이 참조하는 시그니처를 그대로 만듦, 새 테스트는 먼저 씀, `@MockitoBean` 사용, Java 17 문법 범위만 사용)와 `web/AGENTS.md`(카드 구성 요소, 배지 3종, 접근성, 픽스처 모드 표시, `ApiResponse.data` 언래핑, 화면 하나가 끝나면 `npm run build` 확인 후 보고하고 멈춤)를 두어 계층 구조를 보여 줍니다.

### 3-2. 스킬 9개 (`.agents/skills/<name>/`)

각 스킬은 `SKILL.md`(frontmatter `name`, `description`) + `agents/openai.yaml`(`interface.display_name`, `short_description`, `default_prompt`는 `$skill-name`을 포함) 구조입니다. 시스템 스킬 `skill-creator`의 `scripts/init_skill.py <name> --path .agents/skills --resources scripts,references`로 생성하고 `quick_validate.py`로 검증합니다.

| 스킬 | 담는 것 | 포장 문장 |
| --- | --- | --- |
| `spring-convention` | oddventure 관례 요약: 패키지 구조(`common`, `domain/<기능>/{controller,service,model,dto/{request,response},exception}`), Lombok 허용 목록(`@RequiredArgsConstructor`, `@Getter`, `@Builder`, `@Slf4j`; `@Data`·`@Setter` 금지), DTO는 record + 정적 팩토리 `from`, 검증 애노테이션의 한국어 `message`, `ApiResponse` 정적 팩토리, `ErrorCode` enum + `Exception` 쌍, 서비스 애노테이션 순서, 테스트 스타일(`@DisplayName` 한국어, `기능_success`/`기능_fail_사유`, given·when·then 주석, `@ExtendWith(MockitoExtension.class)`, `@WebMvcTest` + `@MockitoBean`). `references/examples.md`에 컨트롤러·서비스·테스트 예시 각 1개 | "팀이 쓰던 Spring 관례를 스킬로 만들어 Codex가 우리 스타일로 코드를 쓴다" |
| `animal-api` | v2 base URL과 오퍼레이션 5개, 파라미터, 필드표와 함정(콤마 소수점, 60일미만, 공란 필드, `neuterYn` 미상 22%, http 사진), 생존 판별 트릭(`SERVICE_KEY_IS_NOT_REGISTERED_ERROR` vs `NO_OPENAPI_SERVICE_ERROR`). `scripts/health.mjs`, `scripts/fetch-all.mjs`, `scripts/coverage.mjs` | "공공 API의 데이터 함정을 스킬로 문서화해서 Codex가 매번 같은 실수를 안 한다" |
| `matching-rules` | 하드 필터 5, 플래그 4, 점수표, 파생 변수, 등급 규칙을 `references/rules.md`에. 규칙 추가 절차(표 행 추가 → `Rules.ALL` 항목 → 테스트 → `rule-auditor` 위임) | "설계 문서를 스킬로 바꿔 두면 Codex가 코드를 생성하고, 규칙 변경도 절차대로 간다" |
| `settle-plan` | 정착 플랜 입력·출력 스키마, 첫 줄 분기, 근거 밖 주장 금지, 14일차 설문 | "파양의 3분의 2가 14일 안에 일어나는 구간을 겨냥한 플랜 계약" |
| `persona-smoke` | `./gradlew smoke`(`PersonaSmokeTest`)로 `fixtures/personas.json` 20명을 규칙에 돌림. 불변식: 맹견 0건, 아이 + 입질 0건, 임대인 반대는 안내 상태, 체중 미기재는 배지, 특징란 공란은 정보 부족 등급, 플래그는 잘 맞음 등급에 없음. 실패한 페르소나와 원인 파일을 표로 정리하는 절차 | "사람이 단계 끝마다 입양자 20명 시나리오를 돌려 검증한다. 실패하면 이 스킬이 원인을 표로 정리한다" |
| `evidence-cite` | `docs/evidence.md`의 근거표(출처, 확인된 사실, 등급 A/B/C)와 인용 규칙, 금지 수치 목록, 발표·UI 문구 작성 규칙 | "지어낸 수치 0. 모든 문장은 근거표에서만 나온다" |
| `demo-pitch` | `docs/pitch.md`에서 `presentations` 플러그인으로 슬라이드 생성, `browser` 플러그인으로 화면 스크린샷, Q&A 카드 형식 | "발표 자료도 Codex가 만들었다" |
| `ui-smoke` | `browser` 플러그인(`browser_use`)으로 `npm run dev`(web) + `./gradlew bootRun` 화면을 실제로 조작: 질문 9개 응답 → 결과 3등급 확인 → 카드의 전화 링크 존재 → 임대인 반대 안내 → 정착 플랜. 화면마다 스크린샷을 `docs/screens/`에 저장하고 깨진 요소를 보고. 브라우저 도구가 안 되면 Playwright 스크립트 `scripts/ui-smoke.mjs`로 대체 | "Codex가 코드를 쓰고 끝내는 게 아니라 서비스를 직접 써 보고 고친다" |
| `ship` | `./gradlew test smoke` → `node scripts/check.mjs`(호출 문장에 "quick"이 있으면 `--quick`) → `codex review --uncommitted` 결과 반영(지적이 셋 이상이면 하나만 고치고 나머지는 `docs/qa.md` 끝 "알려진 이슈"에 기록) → 커밋(`feat: 한글 설명` 형식) → `git push` | "커밋 전 Codex 리뷰 게이트" |

### 3-3. 서브에이전트 4개 (`.codex/agents/<name>.toml`)

```toml
name = "evidence-researcher"
description = "근거 조사 전용. 주장 하나를 받아 출처 URL, 확인된 사실, 등급(A/B/C)을 표 한 줄로 돌려준다."
developer_instructions = """
읽기 전용이다. 파일을 수정하지 않는다.
주장을 받으면 공식 기관·학술 출처를 우선 검색하고, 확인된 문장만 인용한다.
확인하지 못한 수치는 '확인 불가'로 표시한다. 결과는 | 출처 | 확인된 사실 | 등급 | 한 줄이다.
"""
sandbox_mode = "read-only"
model_reasoning_effort = "high"
```

| 에이전트 | 역할 | 현장 사용 장면 |
| --- | --- | --- |
| `evidence-researcher` | 위 예시. 웹 조사 | 근거표의 주장 1건을 현장에서 다시 확인하는 시연(`02-evidence-demo`) |
| `rule-auditor` | 읽기 전용. `Rules.ALL`과 `docs/evidence.md`를 대조해 근거 없는 규칙, 방향이 반대인 규칙(예: 2~7세 가산과 Powell 2021)을 보고 | 규칙 커밋 전 감사 |
| `persona-tester` | `workspace-write`. `./gradlew smoke` 실행 후 이상 결과와 원인 파일 위치 보고 | 사람이 돌린 스모크가 실패했을 때 위임 |
| `ui-builder` | `web/AGENTS.md`를 따르는 화면 작업 전용 worker | 팀원이 서버 작업과 병렬로 화면 스레드를 돌릴 때 |

Best-of-N 장면(가중치 3안을 `worker` 3개에 병렬로 맡기고 `rule-auditor`가 고르는 것)은 현장 120분에서 뺐습니다. 3-7 선택 사항에 남겨 둡니다.

### 3-4. 훅 (`.codex/hooks.json` + `.codex/hooks/*.mjs`)

훅은 두 개만 둡니다. 테스트를 자동으로 돌리거나 턴을 막는 Stop 훅은 두지 않습니다. 테스트 실행은 사람이 합니다.

```json
{
  "hooks": {
    "SessionStart": [{ "hooks": [{ "type": "command", "command": "node .codex/hooks/session-start.mjs", "statusMessage": "이름표 상태 확인", "timeout": 20 }] }],
    "PostToolUse": [{ "matcher": "apply_patch", "hooks": [{ "type": "command", "command": "node .codex/hooks/post-edit-compile.mjs", "timeout": 60 }] }]
  }
}
```

- `session-start.mjs`: `additionalContext`로 인증키 유무(마스킹), 픽스처 건수와 갱신 시각, `build/libs/*.jar` 존재 여부, 마지막 테스트 결과, `docs/CHECKLIST.md`에서 체크되지 않은 항목을 출력합니다. 세션을 열 때마다 "에이전트가 현재 상태를 먼저 읽는다"는 장면이 됩니다.
- `post-edit-compile.mjs`: stdin의 `tool_input`에 `src/main/java` 또는 `src/test/java` 경로가 포함되면 `./gradlew -q compileJava compileTestJava --offline`만 돌리고 컴파일 오류(앞 30줄, 골격이 참조하는 미생성 클래스 목록이 핵심)를 `additionalContext`로 돌려줍니다. 테스트는 돌리지 않습니다. 편집 1회당 몇 초 안에 끝나고, Java 21 문법 사용도 여기서 걸립니다. 그 외 경로면 즉시 종료합니다.
- 첫 실행 때는 `tool_name` 값을 로그 파일에 남기는 임시 훅을 먼저 돌려 실제 매처 이름(`apply_patch`인지, `Bash`인지, 다른 이름인지)을 확인한 뒤 매처를 확정합니다.

### 3-5. 플러그인 (`plugins/nametag/`)

- 시스템 스킬 `plugin-creator`의 `scripts/create_basic_plugin.py nametag --path plugins --marketplace-path .agents/plugins/marketplace.json --with-marketplace --with-skills --with-hooks --with-mcp`로 생성하고 `validate_plugin.py`로 검증합니다.
- `.codex-plugin/plugin.json`: `skills: "./skills/"`, `mcpServers: "./.mcp.json"`, `hooks: "./hooks/hooks.json"`, `interface.displayName: "이름표"`, `category: "Productivity"`, `defaultPrompt` 3개("$matching-rules로 규칙 하나 추가", "서울 강남구 원룸 초보에게 맞는 개 찾기", "$persona-smoke 실행").
- `scripts/build-plugin.mjs`가 `.agents/skills/*`를 `plugins/nametag/skills/`로 복사하고 `.mcp.json`에 MCP 서버(`java -jar ... --spring.profiles.active=mcp`)를 씁니다(중복 편집 방지).
- 설치 데모: `codex plugin marketplace add ./` → `codex plugin list`에 `nametag@nametag-local`이 보이는 것까지. 포장 문장: "보호소 담당자나 다른 팀이 `codex plugin add` 한 줄로 이 매칭 엔진과 워크플로를 가져간다".

### 3-6. 체크리스트 (`docs/CHECKLIST.md` + `scripts/check.mjs`)

완료 정의는 규칙 엔진 테스트 6개와 체크리스트 8항목입니다. 테스트 6개는 키트에 들어 있고, 체크리스트는 사람이 단계 끝마다 스크립트를 돌려 직접 체크합니다. Codex는 체크리스트를 읽고 구현하지만 체크하지 않습니다.

`docs/CHECKLIST.md` 구성:

1. 목적 한 줄: 골격과 테스트가 요구하는 클래스 21개를 만들어 성공 조건 8개를 만족시킨다.
2. 수정 금지 목록: 키트 골격 파일(산출물 구조의 [키트] 표시), `src/test/java`의 테스트 6개, `fixtures/`, `web/`(백엔드 스레드 기준). 공통 규칙: `$spring-convention` 관례, Java 17 문법 범위, 한 프롬프트는 한 단계만.
3. 만들 클래스 표 21개: 패키지, 클래스 이름, 공개 메서드 시그니처, 그것을 참조하는 테스트·골격 파일. 참조 구현에서 그대로 뽑습니다. Codex가 이 표와 컴파일 오류만 보고도 무엇을 만들지 알 수 있어야 합니다.
4. 단계 3개. 단계마다 붙여 넣을 프롬프트 파일, 사람이 돌리는 명령, 체크 상자(`- [ ]`), 목표 시각, 커밋 메시지를 적습니다.

```text
## 1단계 로직 (prompts/03-core.md, 40분까지)
- [ ] ./gradlew -q test smoke --offline 통과
- [ ] node scripts/check.mjs --quick → CHECK 1 PASSED
- [ ] git commit -m "feat: 규칙 엔진 로직 13개 구현"
## 2단계 서버 (prompts/04-server.md, 60분까지)
- [ ] MatchControllerTest 포함 ./gradlew -q test smoke --offline 통과
- [ ] 픽스처 모드 bootRun에서 curl POST /api/v1/matches → success true
- [ ] git commit -m "feat: 매칭·정착 플랜·평가 API 구현"
## 3단계 MCP (prompts/06-mcp.md, 70분까지)
- [ ] node scripts/check.mjs → ALL CHECKS PASSED
- [ ] codex mcp list 에 nametag enabled
- [ ] git commit -m "feat: MCP 도구 5개 구현"
```

5. 성공 조건 8개(스코어보드 줄과 동일): 단위 테스트 전부 통과, 페르소나 스모크 20/20, 근거 없는 문장 0건, 하드 필터 위반 0건, 잘 맞음 등급 내 확인 플래그 0건, 보호 종료 개체 추천 0건, `POST /api/v1/matches` 성공 응답, MCP 도구 5개 응답.
6. MCP 도구 5개(`search_animals`, `match`, `shelter_questions`, `settle_plan`, `evaluate_match`)와 리소스 2개의 이름·설명·입력 필드 표.
7. AGENTS.md 완료 정의는 성공 조건 8개와 같은 내용이어야 합니다.

`scripts/check.mjs`의 동작(사람이 창 B에서 실행):

- `--quick`(1·2단계 끝, 20초 안팎): `./gradlew -q test smoke --offline`을 실행하고 `build/test-results/{test,smoke}/*.xml`에서 통과·실패 수를 센 뒤, `PersonaSmokeTest`가 쓴 `build/evaluation.json`에서 지표를 읽습니다. 전부 통과하면 `CHECK 1 PASSED`.
- 전체(3단계 끝, 60초 안팎): 빠른 모드에 더해 다음 세 가지를 합니다. `./gradlew -q bootJar --offline`. `java -jar build/libs/nametag-0.0.1-SNAPSHOT.jar --spring.profiles.active=evaluate`의 stdout JSON 한 줄(`NAMETAG_USE_FIXTURE=1`). 같은 jar를 `--server.port=18080`과 픽스처 모드로 띄워 `/api/v1/health`가 뜨면 `fixtures/match.request.json`으로 `POST /api/v1/matches`를 호출해 `success: true`를 확인하고 종료. 같은 jar를 `--spring.profiles.active=mcp`로 띄우고 stdin으로 JSON-RPC `initialize` → `notifications/initialized` → `tools/list`를 보내 도구 수가 5인지 확인하고 종료(제한 시간 30초). 전부 통과하면 `ALL CHECKS PASSED`.
- 숫자는 전부 실행 결과로 계산하고, 하나라도 실패하면 `CHECKS FAILED`와 위반 목록을 출력하고 종료 코드 1을 냅니다.

```text
이름표 체크리스트 스코어보드 (전체)
✓ 규칙 엔진 테스트        12/12
✓ 페르소나 스모크         20/20
✓ 근거 없는 문장          0건
✓ 하드 필터 위반          0건
✓ 잘 맞음 등급 내 플래그  0건
✓ 보호 종료 개체 추천     0건
✓ POST /api/v1/matches    응답
✓ MCP 도구 응답           5/5
• 특징란 커버리지         (측정값)%
ALL CHECKS PASSED
```

데모 장면: 사람이 단계마다 돌린 스코어보드 3장(1·2단계 quick, 3단계 전체), 체크 상자가 채워진 `docs/CHECKLIST.md`, 단계별 커밋 3개가 찍힌 `git log`. "모델의 자신감이 아니라 사람이 돌린 테스트·로그·커밋이 증거"라고 말합니다.

### 3-7. 선택 사항 (시간이 남으면)

- `$imagegen`으로 로고 1장 → `web/public/logo.png`.
- `visualize` 플러그인으로 규칙 민감도(가중치 ±1일 때 상위 5장 변화)와 특징란 커버리지 차트.
- REST Docs + `restdocs-api-spec`으로 `/api/v1/*` 문서 생성(oddventure 파이프라인). 컨트롤러 테스트에 `andDo(document(...))`만 붙이면 됩니다.
- `web/dist`를 `src/main/resources/static/`으로 복사해 jar 하나로 화면까지 서빙(단일 URL 데모).
- `codex cloud exec "fixtures/personas.json에 페르소나 10명 추가하고 스모크 통과시켜"`로 클라우드 위임 장면. GitHub 저장소와 클라우드 환경이 있어야 하므로 실패하면 건너뜁니다.
- 현장 120분에서 뺀 연출 세 가지. 00-kickoff 플랜 모드(`/plan`으로 `docs/design.md`를 다시 만드는 장면), 근거표 8건 서브에이전트 팬아웃(`02-evidence-fanout`), Best-of-N 가중치 3안(현행, 활동량 ±1, 지출·기대치 승격을 `worker` 3개에 동시에 맡기고 `rule-auditor`가 `check.mjs` 지표와 근거표로 한 안을 고름. 각 안은 `Rules.java`만 바꿈. 클라우드면 `codex cloud exec --attempts 3`). 리허설이 100분 전에 끝났을 때만 붙입니다.
- WebMCP(`05b-webmcp`, 2-4): 팀원 화면이 80분 안에 끝났을 때만 붙입니다.

## Phase 4. 문서와 현장 프롬프트 (Codex, 약 30분)

### 4-1. `docs/`

| 파일 | 내용 | 원본 |
| --- | --- | --- |
| `design.md` | 설계 문서를 저장소 기준으로 다시 씀(문제 수치, 사용 필드, 질문 9개와 `Profile` 필드 매핑, 규칙, 재파양 방어표, 넣지 않은 것) | `유기견-입양-매칭-설계.md` |
| `evidence.md` | 근거표(출처, 확인된 사실, 등급) + 금지 수치 목록 | `유기견-입양-매칭-판단-기준-아이디어.md`의 근거표 |
| `api.md` | v2 오퍼레이션, 파라미터, 필드, 함정, 생존 판별법, 발급 절차, 우리 REST 엔드포인트 5개 | `국가동물보호정보시스템-API-조사.md` |
| `pitch.md` | 발표 전략(아래 5절) | 신규 |
| `qa.md` | Q&A 방어 카드 | 신규 |
| `codex-playbook.md` | Codex 기술 어필 맵과 포장 전략(아래 6절) | 신규 |
| `timeline.md` | 현장 타임라인과 역할 분담(아래 7절), 시각별 지시 대본(아래 8절) | 신규 |
| `CHECKLIST.md` | 수정 금지 목록, 만들 클래스 표 21개, 단계 3개의 체크 상자, 성공 조건 8개, MCP 도구 표(3-6) | 신규 |

### 4-2. `prompts/` (현장에서 붙여 넣는 프롬프트)

`gpt-5-4-prompting` 스킬의 블록 구조(`<task>`, `<compact_output_contract>`, `<default_follow_through_policy>`, `<verification_loop>`, `<grounding_rules>`, `<action_safety>`)로 씁니다. 각 파일 머리에 실행 주체, 예상 소요, 선행 조건, 붙여 넣을 창(8절의 창 이름)을 적습니다. 모든 구현 프롬프트의 `<default_follow_through_policy>`는 "질문 대신 가정을 적고 진행한다"이고 `<compact_output_contract>`는 "끝나면 변경 파일 목록과 실행한 명령 결과를 보고하고 멈춘다. 다음 단계는 만들지 않는다"입니다. 셸 명령만 있는 파일(`00-setup`, `06-mcp-demo`의 앞부분, `07-plugin`)은 Codex 프롬프트가 아니라 사람이 터미널에 치는 명령 목록입니다. 붙여 넣을 문장의 원문은 8절이고, 파일은 8절을 그대로 옮긴 것입니다.

현장 주 경로(8절 순서):

| 파일 | 내용 | 시각 | 연출 |
| --- | --- | --- | --- |
| `00-setup.md` | 키트 반입, `.env`, `git init`, GitHub push, 의존성 캐시 확인, `node scripts/health.mjs`, `codex` 시작 명령 목록(내 것과 팀원 것) | 0~10 | SessionStart 훅이 상태를 읽는 장면 |
| `03-core.md` | 로직 클래스 13개를 키트 테스트에 맞춰 만드는 프롬프트(`$spring-convention`, `$matching-rules`, `$animal-api`) | 10 | 컴파일 훅, 사람이 돌리는 스코어보드 1장 |
| `05-web.md` | 팀원 화면 프롬프트 4개(질문 → 결과 → 안내 → 플랜, 한 번에 하나씩)와 `mock-api.mjs` 실행 | 10 | 병렬 스레드 |
| `04-server.md` | 서비스 3개 + 컨트롤러 3개 + `MatchControllerTest` + curl 확인 | 40 | 스코어보드 2장 |
| `06-mcp.md` | `NametagTools` `@Tool` 5개 + `bootJar` + stdout 오염 점검 | 60 | 스코어보드 3장(전체) |
| `06-mcp-demo.md` | `mcp.toml.part` 이어 붙이기 → 새 세션 데모 문장 2개 | 70 | "에이전트 시대 대비" 장면 |
| `07-plugin.md` | `node scripts/build-plugin.mjs` → `codex plugin marketplace add ./` → `codex plugin list` | 75 | 배포 가능성 장면 |
| `08-pitch.md` | `$ui-smoke` 지시문(나) + `$demo-pitch` 지시문(팀원) | 80 | Codex가 서비스를 직접 써 보는 장면, 발표 자료 생성 장면 |
| `09-ship.md` | `$ship` | 90 | 리뷰 게이트 장면 |
| `02-evidence-demo.md` | 주장 1개를 `evidence-researcher`에 위임해 `docs/evidence.md` 한 행 갱신 | 92 | 서브에이전트 위임 장면(1분) |

선택(시간이 남을 때):

| 파일 | 내용 | 용도 |
| --- | --- | --- |
| `05b-webmcp.md` | WebMCP 도구 2개 + 폼 선언형 속성 + Inspector 확인(2-4) | 화면이 80분 안에 끝났을 때 |
| `00-kickoff.md` | `/plan`으로 `docs/design.md` 재생성 | 시간이 남을 때의 연출 |
| `02-evidence-fanout.md` | 주장 8개 서브에이전트 팬아웃 | 시간이 남을 때의 연출 |

### 4-3. `scripts/export-kit.sh`

현장 반입용. `bash scripts/export-kit.sh <새 폴더>`로 실행하며, 새 폴더에 `AGENTS.md`, `src/AGENTS.md`, `web/AGENTS.md`, `build.gradle`, `settings.gradle`, `gradlew`, `gradlew.bat`, `gradle/wrapper/`, `src/main/resources/application*.yml`, `src/main/resources/{rules,evidence}/`, `web/package.json`, `web/package-lock.json`, `web/vite.config.ts`, `web/tsconfig*.json`, `web/index.html`, `.agents/`, `.codex/`, `plugins/`, `fixtures/`, `prompts/`, `docs/`(`screens/` 제외), `scripts/`, `.env.example`, `.gitignore`만 복사합니다. `src/main/java`와 `src/test/java`는 산출물 구조에 [키트]로 표시된 파일만 복사합니다(스크립트에 경로 목록을 적어 둠). 로직 클래스 13개, 서비스·컨트롤러 6개, `MatchControllerTest`, `NametagTools`, `web/src`는 복사하지 않습니다. 복사 직후 키트는 컴파일되지 않는 상태가 정상이며, `./gradlew dependencies --offline`으로 의존성 캐시만 확인합니다. 복구가 필요할 때 참조 구현에서 파일 단위로 가져옵니다(8절의 복구 명령).

## 5. 발표 전략 (`docs/pitch.md`에 담을 내용)

### 5-1. 한 문장

"성격이 맞는 개를 찾아 주는 것이 아니라, 국내 파양 사유 1·2·3위를 입양 전에 미리 제거하고, 파양의 3분의 2가 일어나는 첫 2주를 플랜으로 잇는 도구입니다."

### 5-2. 흐름 (7분 기준)

1. 문제 30초: 17.9%가 파양을 고려했고 사유 1위는 행동 문제 42.7%. 공고에는 사진과 품종뿐. 믹스견 79.2%, 안락사율 34.6% 대 7.1%.
2. 데모 4분: 질문 1분 완주 → 결과 화면(3등급, 배지, 보호소에 물을 질문, 전화 버튼) → 임대인 반대 안내 화면 → 정착 플랜 → Codex 세션에서 MCP 도구 호출 30초 → (WebMCP를 붙였을 때만) 같은 화면을 WebMCP Inspector가 도구로 조작하는 장면 20초.
3. 만든 방법 1분 30초: 완료 정의를 테스트 6개와 체크리스트로 먼저 적어 두고 사람이 단계마다 검증, 팀 관례와 설계를 스킬로, 근거 조사를 서브에이전트로, 엔진을 MCP와 WebMCP로, 배포를 플러그인으로. 화면에 체크 상자가 채워진 `docs/CHECKLIST.md`, `check.mjs` 스코어보드, `git log`의 단계별 커밋 3개, `/skills` 목록, `codex mcp list`를 띄운다. 메시지는 "Codex에 코드를 시킨 것이 아니라, 완료 정의를 쥔 사람이 단계마다 검증하며 Codex를 운전할 수 있는 저장소를 만들었다"이다.
4. 다음 단계 30초: 14일차 설문으로 피드백 루프, 보호소 통화 5문항 재판정, A/B 검증 설계.

### 5-3. 심사위원별 포인트

- 비영리단체 대표: 걸러내지 않고 정렬한다(Weiss 2014, 96% 대 96%). 특징란이 빈 개도 "정보 부족" 등급으로 노출해 가장 급한 개체군이 밀리지 않는다. 카드마다 보호소 전화 1통으로 이어진다. 임시보호 출구.
- OpenAI Korea 현직자: 규칙은 코드, LLM은 문장. 이유 문장은 적용 규칙 목록 밖 주장을 검증 단계에서 버린다. 같은 엔진을 사람(웹)과 에이전트(MCP, WebMCP)가 쓴다. 체크리스트의 성공 조건이 곧 평가 지표라서 "사람이 검증하고 에이전트가 고치는 루프"를 스코어보드와 커밋 기록으로 보여 준다. 스킬·서브에이전트·훅·플러그인이 실제로 동작한다.

### 5-4. Q&A 방어 카드 (`docs/qa.md`)

| 질문 | 답 | 근거 |
| --- | --- | --- |
| 판정 기준의 근거는? | 규칙마다 등급 A/B/C를 붙인 근거표가 있다 | `docs/evidence.md` |
| 개 성격을 어떻게 아나? | 모른다고 전제한다. 그래서 "성격 궁합"이 아니라 "파양 사유 제거"로 판정하고, 성격 정보는 보호소 질문으로 넘긴다 | Patronek 2016 (평가 양성 예측도 12%) |
| 왜 마당·평수를 안 보나? | 반환 연구 어디에서도 예측 변수가 아니다. 엄격한 심사가 결과를 좋게 한다는 증거가 없다 | Griffin 2020, Weiss 2014 |
| 큰 개는 아파트에 안 맞지 않나? | 체구는 행동을 설명하지 않는다. 규약 확인 부담일 뿐이라 사용자가 아는 제한을 하드 필터로, 유형은 감점으로 쓴다 | Morrill 2022 |
| LLM이 지어내면? | 입력은 (사람 입력, 개 필드, 적용 규칙)뿐이고 규칙 밖 문장은 검증 실패로 버린다. 키가 없어도 템플릿으로 동작한다 | `ReasonValidator` |
| 효과를 어떻게 증명하나? | 14일차 설문 3문항이 위험 구간과 겹친다. 추천 순서 대 무작위 순서 A/B가 유일한 증명 설계다 | Thumpkin 2022 |
| 특징란이 비어 있는 개가 대부분이면? | 커버리지 숫자를 먼저 말한다(픽스처 스파이크 결과). 그래서 정보 부족 등급과 보호소 질문이 1급 기능이다 | `fixtures/coverage.json` |
| MCP와 WebMCP는 무엇이 다른가? | MCP는 에이전트에게 외부 도구를 주는 규격이고, WebMCP는 웹 페이지 자체가 도구를 노출하는 규격이다. 같은 매칭 엔진을 둘 다로 낸다 | `domain/mcp`, `web/src/webmcp.ts` |
| 완료를 무엇으로 판단했나? | 성공 조건 8개를 스크립트가 재고, 사람이 단계마다 직접 돌려 체크리스트에 표시했다. 모델의 자신감이 아니라 테스트·로그·커밋이 증거다 | `docs/CHECKLIST.md`, `scripts/check.mjs`, `git log` |
| Codex의 Goal 같은 자율 루프는 왜 안 썼나? | 2시간 해커톤에서는 사람이 단계마다 검증하는 편이 예측 가능하고, 심사위원에게 검증 근거를 보여 주기도 쉽다. 같은 체크리스트를 `/goal`에 걸면 자율 루프로도 그대로 돌릴 수 있다 | `docs/CHECKLIST.md` |
| Codex 구성이 보여 주기용으로 억지로 붙인 것 아닌가? | 항목마다 동작 화면이 있다. 스킬 9개, MCP 도구 5개, 서브에이전트 4개로 제한했고 개수로 늘리지 않았다 | `docs/codex-playbook.md` |
| 왜 Spring인가? | 팀이 매일 쓰는 스택이고, 관례를 스킬로 옮겨 Codex가 같은 스타일로 쓴다. 규칙 엔진은 Spring 의존이 없어 어디로든 옮길 수 있다 | `$spring-convention`, `src/AGENTS.md` |
| 골격을 들고 왔으면 Codex가 한 것은? | 골격은 응답 형식·DTO·클라이언트 같은 팀 관례 코드와 완료 정의인 테스트 6개다. 도메인 로직 13개, 서비스·컨트롤러 6개, MCP 도구 5개는 현장에서 Codex가 테스트를 통과시키며 만들었고 커밋 기록으로 보인다 | `git log`, `docs/CHECKLIST.md` 클래스 표 |

## 6. Codex 기술 포장 전략 (`docs/codex-playbook.md`에 담을 내용)

| 별거 아닌 것 | Codex 포장 | 발표 문장 |
| --- | --- | --- |
| 체중 정규식 파서 | `animal-api` 스킬 references + 테스트 + PostToolUse 컴파일 훅 | "데이터 함정을 스킬로 남겨 팀 전체와 에이전트가 재사용" |
| 규칙표 | `matching-rules` 스킬 + `Rules.ALL` 데이터 목록 + `rule-auditor` 감사 | "규칙 한 줄이 곧 코드이고, 바꾸면 감사 에이전트가 근거를 대조" |
| 팀 코딩 관례 | `spring-convention` 스킬 + `src/AGENTS.md` | "팀이 쓰던 관례를 스킬로 옮겨 Codex가 우리 스타일로 쓴다" |
| Spring 골격과 테스트 6개 | 키트로 반입한 완료 정의(테스트가 사양) + 프롬프트 3개 | "완료 정의를 테스트로 먼저 적어 두고, Codex가 60분 안에 통과시켰다" |
| 페르소나 JSON 20개 | `persona-smoke` 스킬 + 사람이 돌리는 `./gradlew smoke` | "단계 끝마다 입양자 20명 시나리오를 통과해야 다음 단계로 간다" |
| API 조사 문서 | `$animal-api` 스킬 + `scripts/health.mjs` 생존 판별 | "구버전 경로가 폐기된 것을 스크립트 한 번으로 걸러내고, 그 함정을 스킬에 남김" |
| 아이디어 문서의 근거표 | `evidence-cite` 스킬 + `evidence-researcher` 서브에이전트(현장에서는 주장 1건 시연) | "근거 조사는 읽기 전용 에이전트에 위임하고, 근거표 밖 수치는 쓰지 않는다" |
| 설계 문서 | `matching-rules` 스킬 + `docs/design.md` + `Profile` 필드 매핑 | "설계 문서가 곧 스킬이라 Codex가 같은 규칙으로 코드를 낸다" |
| 매칭 서비스 | Spring AI MCP 서버 도구 5개 + 리소스 2개 | "어떤 에이전트든 이 매칭을 도구로 호출" |
| 폴더 구조 | 플러그인 + 로컬 마켓플레이스 | "`codex plugin add` 한 줄로 배포" |
| README | AGENTS.md 계층(루트, src, web) | "에이전트 온보딩 문서" |
| 커밋 전 검토 | `$ship` 스킬 안의 `codex review --uncommitted` | "리뷰 게이트를 통과한 커밋만" |
| 병렬 작업 | Codex 스레드 2개(서버·화면) + 서브에이전트 | "사람 2명, 에이전트 6개가 동시에 일함" |
| 로고, 슬라이드 | `$imagegen`, `presentations` 플러그인 | "발표 자료까지 Codex" |
| 완료 조건 목록 | `docs/CHECKLIST.md` + `check.mjs` 스코어보드(사람이 단계마다 실행) + 단계별 커밋 | "완료 정의를 체크리스트로 적어 두면 사람이 검증하고 Codex가 고친다" |
| 지표 계산 클래스 | `Evaluator` + MCP `evaluate_match` 도구 + `GET /api/v1/evaluations` | "Codex가 자기 결과물을 도구로 다시 평가한다" |
| 질문 폼 | WebMCP 선언형 속성 + `registerTool` 2개 | "사람용 웹이 그대로 에이전트용 도구가 된다" |
| 화면 클릭 확인 | `ui-smoke` 스킬(`browser_use`) | "Codex가 서비스를 직접 써 보고 고친다" |
| 가중치 3안 비교(선택) | 서브에이전트 3개 병렬 + `rule-auditor` 선택(클라우드면 `--attempts 3`) | "Best-of-N으로 규칙안을 고른다". 시간이 남을 때만 |

발표에서 용어를 섞지 않도록 역할을 한 줄씩 고정합니다.

| 요소 | 역할 |
| --- | --- |
| AGENTS.md | 저장소 운영 규칙과 완료 정의 |
| Skill | 반복 작업의 절차와 팀 관례 |
| Sub-agent | 조사·감사·검증을 병렬로 분담 |
| Hook | 세션 시작 상태 읽기와 편집 직후 컴파일 확인 |
| MCP | 에이전트에게 매칭 엔진을 도구로 제공 |
| WebMCP | 웹 화면 자체가 에이전트에게 도구를 노출 |
| Checklist | 완료 정의와 사람이 단계마다 하는 검증 절차 |
| Evaluation | 완료 상태를 숫자로 재는 클래스와 도구 |

넣지 않는 것: 개수를 채우려는 스킬, 의미 없는 MCP 도구, 모든 것을 에이전트로 만들기, 코드 생성량 자랑, 자율 루프. 심사위원은 "기능을 위해 만들었는지, 보여 주려고 붙였는지"를 바로 구분합니다.

연출 원칙: 각 항목은 실제로 동작하는 화면(`/skills`, `codex mcp list`, `codex plugin list`, 훅 statusMessage, 스코어보드, 체크된 체크리스트)을 하나씩 보여 준다. 말로만 하는 항목은 넣지 않는다. 키트는 "우리 팀이 쓰는 Codex 워크플로 키트"로 소개하고, 설계·API·근거 문서는 준비해 온 것으로 말한다. 현장에서 처음 만드는 연출은 근거 1건 서브에이전트 시연(`02-evidence-demo`)과 체크리스트 스코어보드로 충분하다.

## 7. 현장 타임라인 (`docs/timeline.md`, 개발 120분 + 발표 준비 30분)

| 시각 | 나(백엔드·MCP) | 팀원(화면·발표) | 체크 |
| --- | --- | --- | --- |
| 0~10 | 키트 반입, `.env`, `git init` → GitHub push, 의존성 캐시 확인, `codex` 시작 | 클론, `npm install --prefer-offline`, `node scripts/mock-api.mjs`, `codex` 시작 | SessionStart 훅 메시지, `/skills` 9개 |
| 10~40 | `03-core` 붙여 넣기 → 끝나면 내가 `test smoke` + `check.mjs --quick` → 체크 → 커밋 | `05-web` 질문 화면 → 브라우저 확인 → 결과 화면 | 40분 CHECK 1 PASSED |
| 40~60 | `04-server` 붙여 넣기 → 끝나면 `test smoke` + curl `POST /api/v1/matches` → 체크 → 커밋 | 안내 화면 → 플랜 화면 | 60분 curl success true |
| 60~70 | `06-mcp` 붙여 넣기 → 끝나면 `check.mjs`(전체) → 체크 → 커밋 | 화면 4개 브라우저 확인, `git push` | 70분 ALL CHECKS PASSED |
| 70~80 | MCP 블록 이어 붙이기 → 새 세션 `codex mcp list` → 도구 호출 데모 2문장 → 플러그인 등록 | `git pull` 후 목 API를 끄고 `./gradlew bootRun`으로 교체 | `codex mcp list`, `codex plugin list` |
| 80~95 | `git pull` → `$ui-smoke` → 깨진 곳 수정 → `$ship` → 근거 1건 시연 | `$demo-pitch` 슬라이드 | 스크린샷 4장 |
| 95~120 | 리허설 2회(픽스처 모드), 백업 스크린샷, 발표용 창 배치 | 발표 대본, 슬라이드 다듬기 | 데모 5분 안에 완주 |
| 120~150 | 발표 준비(슬라이드 확정, 역할, 창 배치 재확인) | 같이 | |

- 확인 지점 세 개. 40분에 `check.mjs --quick`, 60분에 curl, 70분에 `check.mjs` 전체가 통과하지 않으면 참조 구현에서 빠진 파일을 채웁니다(8절 복구 명령). 판단은 시각으로만 하고, 70분 이후에는 새 코드를 시작하지 않습니다.
- 막히면 그 자리에서 말하고 15분마다 상태를 맞춥니다. 참조 구현에서 복구할 때는 파일 단위로 가져오고 커밋 메시지에 남기지 않습니다.
- 크레딧 100달러 배분: 구현 스레드 둘 다 `-c model_reasoning_effort="high"`, `codex review`와 발표 자료만 기본값 xhigh. 서브에이전트 동시 3개 이하. `service_tier = "fast"` 유지.

## 8. 현장 지시 대본 (`docs/timeline.md`와 `prompts/`에 문장 그대로 옮길 내용)

### 8-0. 창 배치

내 노트북 창 세 개, 팀원 노트북 창 세 개입니다. 모두 저장소 루트(`~/hack/nametag`)에서 엽니다(루트 `AGENTS.md`가 읽히도록).

| 창 | 위치 | 용도 |
| --- | --- | --- |
| 창 A | 내 노트북 터미널 1, `codex -c model_reasoning_effort="high"` | 백엔드 스레드. 프롬프트 3개를 순서대로 붙여 넣는 곳 |
| 창 B | 내 노트북 터미널 2, 일반 셸 | `git`, `curl`, `bootRun`, `check.mjs`, 복구 명령. 체크리스트 확인은 여기서 |
| 창 C | 내 노트북 터미널 3, 70분에 새로 여는 `codex` | MCP 데모 전용 세션(MCP 서버 목록은 세션 시작 때 읽히므로 새 세션이 필요) |
| 창 D | 팀원 노트북 터미널 1, `codex -c model_reasoning_effort="high"` | 화면 스레드. 화면 프롬프트 4개를 하나씩 붙여 넣는 곳 |
| 창 E | 팀원 노트북 터미널 2 | `node scripts/mock-api.mjs`, 70분부터 `./gradlew bootRun` |
| 창 F | 팀원 노트북 터미널 3 | `cd web && npm run dev`, 브라우저(화면 확인은 사람이 눈으로) |

원칙: Codex가 한 프롬프트를 처리하는 동안은 그 창에 아무것도 치지 않습니다. Codex가 "보고하고 멈추면" 사람이 창 B(또는 창 F)에서 확인 명령을 돌리고, 체크리스트에 표시하고, 커밋한 뒤 다음 프롬프트를 붙여 넣습니다. 아래에서 "붙여 넣기"는 그 창에 그대로 입력하는 문장이고, 코드 블록의 셸 명령은 창 B 또는 창 E에 칩니다.

### 8-1. 0~10분: 셋업

내가 창 B에서 순서대로 칩니다(`prompts/00-setup.md`).

```bash
REF=/Users/hylee/swm/codex-impact-workshop/nametag
bash $REF/scripts/export-kit.sh ~/hack/nametag && cd ~/hack/nametag
cp .env.example .env               # DATA_GO_KR_SERVICE_KEY, OPENAI_API_KEY 값 채우기
node scripts/health.mjs            # 키 반영 확인. 실패하면 .env에 NAMETAG_USE_FIXTURE=1 추가
./gradlew dependencies --offline -q > /dev/null && echo "gradle cache ok"   # 골격이 아직 컴파일되지 않으므로 build 대신 의존성 해석만 확인
git init && git add -A && git commit -m "chore: 이름표 Codex 키트 반입"
gh repo create nametag --private --source=. --push
```

이어서 창 A에서 `codex -c model_reasoning_effort="high"`. SessionStart 훅이 키 유무, 픽스처 건수, jar 없음, 체크되지 않은 체크리스트 항목을 출력합니다. `/skills`로 스킬 9개가 보이는지 봅니다. 여기까지 5분.

팀원이 창 E에서 칩니다.

```bash
git clone <저장소 URL> ~/hack/nametag && cd ~/hack/nametag
cp .env.example .env && echo NAMETAG_USE_FIXTURE=1 >> .env
cd web && npm install --prefer-offline && cd ..
node scripts/mock-api.mjs          # 8080에서 픽스처 응답. 이 창은 그대로 둠
```

창 F에서 `cd web && npm run dev`, 창 D에서 `codex -c model_reasoning_effort="high"`.

안 될 때: `gh repo create`가 실패하면 GitHub 웹에서 빈 저장소를 만들고 `git remote add origin <URL> && git push -u origin main`. 의존성 해석이 `--offline`에서 실패하면 `--offline`을 빼고 한 번 돌립니다.

### 8-2. 10분: 첫 프롬프트 (창 A, 창 D)

창 A에 붙여 넣기(`prompts/03-core.md`).

```text
$spring-convention $matching-rules $animal-api
docs/CHECKLIST.md의 1단계를 수행해. 키트에 있는 골격 파일과 src/test/java의 테스트 6개, fixtures/는 수정하지 말고, 컴파일 오류와 테스트가 요구하는 로직 클래스 13개(AnimalParser, AnimalDeriver, HardFilters, CheckFlags, Rules, Scorer, Grader, ShelterQuestions, ReasonTemplates, ReasonValidator, MatchEngine, SettlePlanBuilder, Evaluator)를 docs/CHECKLIST.md 클래스 표의 패키지와 시그니처 그대로 만들어. Spring 의존 없는 순수 Java 17 문법 범위로 쓰고, 규칙과 파생 변수는 $matching-rules의 references/rules.md를 Rules.ALL과 AnimalDeriver로 옮겨. 만드는 중간에 ./gradlew -q test smoke --offline을 돌려 실패를 고치고, 전부 통과하면 변경한 파일 목록과 마지막 테스트 결과를 보고하고 멈춰. 질문이 생기면 묻지 말고 가정을 적고 진행해. 서비스·컨트롤러·MCP는 만들지 마.
```

창 D에 붙여 넣기(`prompts/05-web.md`의 첫 번째).

```text
web/AGENTS.md를 따라 web/src에 질문 화면 하나만 먼저 만들어. web/만 수정해. API는 http://localhost:8080이고 지금은 node scripts/mock-api.mjs가 같은 형식으로 응답하므로 fixtures/match.sample.json과 fixtures/settle-plan.sample.json의 필드를 그대로 써. 응답은 항상 ApiResponse 래퍼이므로 data를 풀어서 써. 상단에 픽스처 모드 표시를 둬. 질문 화면은 docs/design.md의 9문항이고 지역은 /api/v1/regions/sido와 /api/v1/regions/sigungu에서 불러오며, 제출하면 POST /api/v1/matches를 호출하고 응답을 콘솔에 찍어. 끝나면 npm run build가 통과하는지 확인하고 변경 파일 목록을 보고한 뒤 멈춰. 다음 화면은 내가 따로 지시한다. 질문이 생기면 묻지 말고 가정을 적고 진행해.
```

확인할 것: 창 A에서 `apply_patch` 뒤에 컴파일 훅이 몇 초 도는 것이 보입니다. 처음 몇 턴은 컴파일 오류가 길게 나오는 것이 정상입니다(골격이 참조하는 클래스가 아직 없음).

### 8-3. 10~40분: 1단계 로직 (창 A는 Codex가 멈출 때까지 기다림)

- 25분: 창 A의 진행 로그에서 `AnimalParser`, `AnimalDeriver`, `HardFilters`가 생겼으면 정상 속도입니다. 그동안 팀원 화면을 봐 줍니다.
- Codex가 보고하고 멈추면 창 B에서 확인하고 체크리스트에 표시합니다.

```bash
./gradlew -q test smoke --offline && node scripts/check.mjs --quick     # CHECK 1 PASSED
# docs/CHECKLIST.md 1단계의 상자 두 개에 [x]
git add -A && git commit -m "feat: 규칙 엔진 로직 13개 구현" && git push
```

- 테스트가 실패하면 실패 메시지를 창 A에 붙여 넣습니다. 예: `AnimalParserTest의 parseKg_success가 실패한다. 실패 메시지: <붙여 넣기>. 공백 제거 → 정규식 ^([\d.,]+)\(?Kg\)? → 콤마를 점으로 바꾸는 순서로 고치고 다시 돌린 결과를 보고해.` 같은 실패가 두 번 반복되면 그 클래스는 참조 구현에서 가져옵니다.
- **40분 확인 지점**: `CHECK 1 PASSED`가 아니면 창 B에서 로직 클래스를 복구합니다.

```bash
REF=/Users/hylee/swm/codex-impact-workshop/nametag
for d in animal match settle evaluate; do
  rsync -a --ignore-existing $REF/src/main/java/com/nametag/domain/$d/model/ src/main/java/com/nametag/domain/$d/model/
done
./gradlew -q test smoke --offline && node scripts/check.mjs --quick
```

`--ignore-existing`이라 Codex가 만든 파일은 남고 빠진 파일만 채워집니다. 테스트가 여전히 실패하면 실패한 클래스만 `cp $REF/src/main/java/.../<클래스>.java`로 덮어씁니다. 복구 뒤 체크·커밋은 위와 같고, 창 A에는 다음 프롬프트를 붙여 넣기 전에 한 줄 알립니다: `model 패키지의 빠진 클래스를 사람이 채웠다. 다시 읽어 둬.`

- 팀원(창 D): 질문 화면이 끝나면 창 F 브라우저에서 9문항이 보이고 지역 목록이 뜨는지 확인한 뒤, 창 D에 두 번째 프롬프트를 붙여 넣습니다.

```text
결과 화면을 만들어. POST /api/v1/matches의 응답 형식은 fixtures/match.sample.json이다. 3등급 섹션(잘 맞음, 조건부, 정보 부족), 카드에는 사진(https), 이름표 형식 요약(품종·나이·체중), 배지("성견 체중 확인 필요", "정보 부족", 확인 플래그 노란 배지), 추천 이유 2~3문장, 보호소에 물을 질문, 보호소 이름·tel: 링크·공고 마감일, "품종으로 성격을 단정하지 않습니다" 한 줄. 끝나면 npm run build 확인 후 보고하고 멈춰.
```

### 8-4. 40~60분: 2단계 서버 (창 A)

창 A에 붙여 넣기(`prompts/04-server.md`).

```text
$spring-convention
docs/CHECKLIST.md의 2단계를 수행해. 1단계에서 만든 model 패키지와 키트 골격, 테스트 6개는 수정하지 마. MatchService, SettleService, EvaluateService와 MatchController, SettlePlanController, EvaluationController, MatchControllerTest(@WebMvcTest + @MockitoBean)를 docs/CHECKLIST.md 클래스 표와 docs/api.md의 REST 절대로 만들어. 컨트롤러는 키트의 DTO와 ApiResponse를 그대로 쓰고 엔드포인트는 /api/v1/matches, /api/v1/settle-plans, /api/v1/evaluations다. ./gradlew -q test smoke --offline이 통과하면 NAMETAG_USE_FIXTURE=1로 bootRun을 띄워 curl -s -X POST localhost:8080/api/v1/matches -H 'Content-Type: application/json' -d @fixtures/match.request.json 의 응답에 success true가 있는지 확인하고 서버를 끈 뒤, 변경 파일 목록과 curl 응답 앞부분을 보고하고 멈춰. 질문이 생기면 묻지 말고 가정을 적고 진행해. MCP는 만들지 마.
```

- 55분: 창 A의 로그에 컨트롤러가 생겼는지 봅니다.
- Codex가 보고하고 멈추면 창 B에서 직접 확인하고 체크합니다.

```bash
./gradlew -q test smoke --offline
NAMETAG_USE_FIXTURE=1 ./gradlew bootRun --offline     # 창 B의 새 탭. 확인이 끝나면 Ctrl+C
curl -s localhost:8080/api/v1/health
curl -s -X POST localhost:8080/api/v1/matches -H 'Content-Type: application/json' -d @fixtures/match.request.json | head -c 600
# docs/CHECKLIST.md 2단계의 상자 두 개에 [x]
git add -A && git commit -m "feat: 매칭·정착 플랜·평가 API 구현" && git push
```

- **60분 확인 지점**: curl이 `success: true`가 아니면 창 B에서 복구합니다.

```bash
REF=/Users/hylee/swm/codex-impact-workshop/nametag
for d in match settle evaluate; do
  rsync -a --ignore-existing $REF/src/main/java/com/nametag/domain/$d/ src/main/java/com/nametag/domain/$d/
done
rsync -a --ignore-existing $REF/src/test/java/ src/test/java/
./gradlew -q test smoke --offline
```

복구 뒤 체크·커밋은 위와 같고, 창 A에 한 줄 알립니다: `서비스·컨트롤러의 빠진 파일을 사람이 채웠다. 다시 읽어 둬.`

- 팀원(창 D): 결과 화면을 브라우저에서 확인한 뒤 세 번째, 네 번째 프롬프트를 차례로 붙여 넣습니다.

```text
임대인 반대 안내 화면을 만들어. POST /api/v1/matches 응답의 landlordDenied가 true면 이 화면으로 간다. 특약 효력, 동의 받는 순서, 다시 시도 버튼. 끝나면 npm run build 확인 후 보고하고 멈춰.
```

```text
정착 플랜 화면을 만들어. 결과 카드에서 개를 고르면 POST /api/v1/settle-plans를 호출하고 응답 형식은 fixtures/settle-plan.sample.json이다. 날짜별 할 일, 지켜볼 위험 신호, 보호소 연락처, 14일차 설문 3문항. 끝나면 npm run build 확인 후 보고하고 멈춰.
```

### 8-5. 60~70분: 3단계 MCP (창 A)

창 A에 붙여 넣기(`prompts/06-mcp.md`).

```text
$spring-convention
docs/CHECKLIST.md의 3단계를 수행해. domain/mcp/tools/NametagTools를 만들어 docs/CHECKLIST.md의 MCP 도구 표대로 @Tool(name = ...) 메서드 5개(search_animals, match, shelter_questions, settle_plan, evaluate_match)를 구현해. 기존 서비스만 호출하고 새 로직을 넣지 마. 키트의 McpResourceConfig와 application-mcp.yml은 수정하지 마. ./gradlew -q test smoke --offline과 ./gradlew -q bootJar --offline이 통과하면, NAMETAG_USE_FIXTURE=1 java -jar build/libs/nametag-0.0.1-SNAPSHOT.jar --spring.profiles.active=mcp 를 띄워 stdin으로 JSON-RPC initialize, notifications/initialized, tools/list를 보내 도구 5개가 나오는지와 stdout에 JSON 외의 출력이 없는지 확인하고 프로세스를 끝낸 뒤, 결과를 보고하고 멈춰. 질문이 생기면 묻지 말고 가정을 적고 진행해.
```

- Codex가 보고하고 멈추면 창 B에서 전체 체크를 돌리고 체크·커밋합니다.

```bash
node scripts/check.mjs                                # ALL CHECKS PASSED, 스코어보드 화면을 남겨 둠
# docs/CHECKLIST.md 3단계의 첫 상자에 [x]
git add -A && git commit -m "feat: MCP 도구 5개 구현" && git push
```

- **70분 확인 지점**: `ALL CHECKS PASSED`가 아니면 창 B에서 남은 파일을 복구합니다.

```bash
REF=/Users/hylee/swm/codex-impact-workshop/nametag
rsync -a --ignore-existing $REF/src/main/java/ src/main/java/
rsync -a --ignore-existing $REF/src/test/java/ src/test/java/
./gradlew -q test smoke --offline && node scripts/check.mjs
```

그래도 MCP 줄만 실패하면 MCP 장면을 발표에서 빼고 스코어보드의 MCP 줄은 "미완"으로 말합니다. `build/mcp.log`에 stdout 오염(배너·로그) 흔적이 있으면 `application-mcp.yml`의 세 설정을 확인합니다.

- 팀원(창 D, 창 F): 화면 4개를 브라우저에서 순서대로 눌러 보고 `git add -A && git commit -m "feat: 화면 4개 구현" && git push`.

### 8-6. 70~80분: MCP 데모와 플러그인 (창 B, 창 C)

창 B(`prompts/06-mcp-demo.md`).

```bash
cat .codex/mcp.toml.part >> .codex/config.toml
codex mcp list                     # nametag enabled → docs/CHECKLIST.md 3단계 둘째 상자에 [x]
```

창 C에서 새 세션 `codex`를 열고 붙여 넣기.

```text
서울 강남구, 원룸, 처음 키우는 사람에게 맞는 개 3마리를 nametag MCP 도구로 찾아서 등급과 이유, 보호소 전화까지 표로 보여 줘.
```

```text
방금 추천을 evaluate_match 도구로 평가해서 지표 표를 보여 줘.
```

두 화면을 스크린샷으로 남깁니다(발표 백업). 이어서 창 B(`prompts/07-plugin.md`).

```bash
node scripts/build-plugin.mjs
codex plugin marketplace add ./
codex plugin list                  # nametag@nametag-local
```

팀원(창 E): `git pull` → 목 API 종료(Ctrl+C) → `NAMETAG_USE_FIXTURE=1 ./gradlew bootRun --offline`. 화면이 실제 백엔드로 붙는지 창 F 브라우저에서 확인합니다.

### 8-7. 80~95분: 통합 검증, 커밋, 근거 1건 시연 (창 A, 창 D)

창 B의 새 탭 두 개: `git pull && cd web && npm install --prefer-offline && npm run dev`, 그리고 `NAMETAG_USE_FIXTURE=1 ./gradlew bootRun --offline`.

창 A에 붙여 넣기(`prompts/08-pitch.md` 앞부분).

```text
$ui-smoke 를 실행해. 백엔드는 localhost:8080(픽스처 모드), 화면은 localhost:5173이다. 질문 9개 → 결과 3등급 → 카드의 tel: 링크 → 임대인 반대 안내 → 정착 플랜 순서로 실제로 조작하고, 화면마다 스크린샷을 docs/screens/에 저장한 뒤 깨진 요소를 목록으로 보고해. 백엔드 쪽 원인이면 src/를 고치고, 화면 쪽 원인이면 고치지 말고 파일 경로와 증상만 적어.
```

화면 쪽 문제는 팀원의 창 D에 그대로 붙여 넣어 고치게 합니다(`web/`은 팀원만 수정). 통과하면 창 A에 붙여 넣기(`prompts/09-ship.md`).

```text
$ship 을 실행해. 커밋 메시지는 "feat: 화면 연동 검증과 문서 갱신"이고 push까지 해.
```

이어서 창 A에 근거 1건 시연(`prompts/02-evidence-demo.md`).

```text
evidence-researcher 서브에이전트에게 docs/evidence.md의 주장 중 등급 C인 것 하나를 골라 근거를 다시 찾게 하고, 돌아온 표 한 줄로 그 행을 갱신해. 확인 불가면 등급을 그대로 두고 "확인 불가"라고 적어.
```

이 장면도 스크린샷을 남깁니다.

팀원(창 D): 붙여 넣기(`prompts/08-pitch.md` 뒷부분).

```text
$demo-pitch 를 실행해. docs/pitch.md와 docs/qa.md, docs/screens/의 스크린샷으로 7분 발표 슬라이드를 만들어. 슬라이드는 문제 1장, 데모 화면 4장, 만든 방법 1장(체크된 CHECKLIST.md, check.mjs 스코어보드, git log, /skills, codex mcp list 화면), 다음 단계 1장이다. 수치는 docs/evidence.md에 있는 것만 쓴다.
```

### 8-8. 95~120분: 리허설

- 창 B와 창 E 모두 `NAMETAG_USE_FIXTURE=1 ./gradlew bootRun --offline`을 띄운 상태로 둡니다. 발표는 픽스처 모드로 합니다(공공 API 장애와 무관).
- 발표 노트북(팀원 노트북)에서 데모 순서를 두 번 완주하며 시간을 잽니다: 질문 1분 → 결과 → 안내 → 플랜 → 창 C의 MCP 대화 → 창 B의 스코어보드와 체크된 `docs/CHECKLIST.md`, `git log --oneline`.
- 창 B에서 `node scripts/check.mjs`를 한 번 더 돌려 스코어보드 화면을 띄워 둡니다. 발표 중에는 다시 돌리지 않습니다(60초 걸림).
- 백업: `docs/screens/` 4장, MCP 대화 2장, 스코어보드 1장, 체크리스트 1장, 근거 시연 1장을 슬라이드 부록에 넣습니다.
- 시간이 남으면 `05b-webmcp`를 창 D에 붙여 넣습니다. 110분 이후에는 새 기능을 시작하지 않습니다.

### 8-9. 120~150분: 발표 준비

- 슬라이드 확정, 발표 역할(문제·데모는 팀원, 만든 방법·Q&A는 나).
- 발표 노트북에 띄워 둘 창: 브라우저(질문 화면), 창 C(MCP 대화 결과), 창 B(스코어보드, 체크된 체크리스트, `git log`), 터미널에 `/skills`·`codex mcp list`·`codex plugin list` 출력. 내 노트북 화면을 발표 노트북으로 옮기기 어려우면 스크린샷으로 대체합니다.
- `docs/qa.md`의 방어 카드를 한 번 읽습니다. "골격을 들고 왔으면 Codex가 한 것은?"과 "자율 루프는 왜 안 썼나?" 카드를 특히 준비합니다.

## 리스크와 대비

| 리스크 | 대비 |
| --- | --- |
| 인증키 미반영, API 장애 | `NAMETAG_USE_FIXTURE=1` 전환. 화면 상단 표시 |
| Gradle 첫 빌드가 느리거나 현장 네트워크가 나쁨 | Phase 0에서 두 노트북 모두 `./gradlew build`로 캐시를 채움. 현장에서는 `--offline`. Gradle 데몬 유지 |
| 훅이 Codex를 느리게 함 | 훅은 세션 시작과 편집 후 컴파일(몇 초)뿐. 테스트는 사람이 단계 끝에 한 번 |
| 훅 매처 이름 불일치 | 첫 실행 때 `tool_name` 로그 훅으로 확인 후 확정 |
| 프롬프트 3개가 60분 안에 못 끝남 | 현장 생성을 21개 파일로 제한. 40분·60분·70분 확인 지점. 참조 구현에서 `rsync --ignore-existing`으로 빠진 파일만 채움(8절). 화면 데모는 목 API로 어차피 가능 |
| Codex가 한 프롬프트에서 다음 단계까지 만들거나 중간에 질문하고 멈춤 | 프롬프트마다 "다음 단계는 만들지 마"와 "질문 대신 가정을 적고 진행"을 명시. 루트 AGENTS.md에도 같은 규칙 |
| 골격·테스트가 기대하는 시그니처와 Codex 코드가 어긋남 | `docs/CHECKLIST.md` 클래스 표에 시그니처를 적고, 골격·테스트 수정 금지를 프롬프트와 `src/AGENTS.md`에 명시. 컴파일 훅이 어긋남을 즉시 알림 |
| 팀원 화면이 백엔드 없이 못 나감 | `scripts/mock-api.mjs`가 픽스처 응답. DTO가 키트에 있어 계약이 고정됨. 70분에 실제 백엔드로 교체 |
| jar 없는 상태에서 MCP 설정이 세션 시작을 막음 | 키트의 `.codex/config.toml`에는 MCP 블록이 없고 `.codex/mcp.toml.part`에 따로 둠. 70분에 이어 붙이고 새 세션 |
| MCP 서버 기동 실패 | jar 직접 실행, `startup_timeout_sec = 60`, 픽스처 모드 고정. stdout 오염(배너·로그)은 `application-mcp.yml`로 차단하고 `build/mcp.log`로 확인 |
| Codex가 다른 스타일의 Java를 씀 | `$spring-convention` 스킬과 `src/AGENTS.md`. `@MockBean` 대신 `@MockitoBean`, JPA 의존 추가 금지, Java 21 문법(switch 패턴 매칭, record 패턴) 금지를 명시. 툴체인이 17이라 컴파일 단계에서도 걸림 |
| 서브에이전트 토큰 과다 | `max_concurrent_threads_per_session = 3`, 조사 에이전트는 `read-only` |
| 프로젝트 `.codex/config.toml` 미인식 | `~/.codex/config.toml`에 `[projects."<경로>"] trust_level = "trusted"` 추가 |
| 팀원이 Codex에 익숙하지 않음 | 프롬프트 파일에 실행 주체와 붙여 넣을 문장을 그대로 적어 둠. 화면 프롬프트는 한 번에 하나씩 |
| WebMCP 플래그 미적용, Inspector 확장 실패 | 콘솔 `getTools()` 화면과 스크린샷을 백업으로. 발표 노트북 1대에서만 시연 |
| `browser_use`가 로컬 화면을 못 잡음 | `scripts/ui-smoke.mjs`(Playwright)로 대체. 그것도 안 되면 스크린샷을 사람이 찍음 |

## 검증

1. `./gradlew test`: 파서 케이스 5개, 나이 케이스 2개, 맹견 2개, 아이 + 입질, 긍정 우선, 플래그 비상쇄, `ReasonValidator`, `MatchController` 슬라이스 테스트 통과. JaCoCo 0.40 이상.
2. `./gradlew smoke`: 페르소나 20명 불변식 전부 통과.
3. `codex` 세션에서 `/skills`에 스킬 9개가 보이고, `$spring-convention`과 `$animal-api` 명시 호출이 동작.
4. `.codex/agents` 확인: "evidence-researcher에게 이 주장의 근거를 찾게 해 줘"로 서브에이전트가 스폰되고 표 한 줄을 돌려줌.
5. 훅: 새 세션 시작 시 상태 메시지(체크되지 않은 항목 포함) 출력, `src/main/java` 편집 후 컴파일 훅이 몇 초 안에 끝남. Stop 훅이 없어서 턴 끝에 아무것도 자동 실행되지 않음.
6. `./gradlew bootJar` 후 `codex mcp list`에 `nametag` enabled. 세션에서 `search_animals`, `match` 호출 결과 확인. `build/mcp.log`에만 로그가 남고 stdout은 깨끗함.
7. `python3 ~/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py plugins/nametag` 통과, `codex plugin list`에 표시.
8. `./gradlew bootRun` + `cd web && npm run dev` → 질문 9개 → 결과 3등급 → 임대인 반대 안내 → 정착 플랜까지 화면 확인. 픽스처 모드와 실제 API 모드 각 1회.
9. 리허설: 새 폴더에 `export-kit.sh`로 키트만 반입하고 8절 대본을 0분부터 95분까지 시계를 보며 그대로 실행해 실제 소요를 `docs/timeline.md`에 반영. 키트 상태에서 `./gradlew dependencies --offline`이 성공해야 함. 프롬프트 3개가 60분을 넘기면 `docs/CHECKLIST.md` 클래스 표에서 서비스·컨트롤러 일부를 키트로 옮겨 현장 생성 분량을 더 줄임.
10. 프롬프트 계약: `03-core`를 붙여 넣었을 때 Codex가 로직 13개만 만들고 서비스·컨트롤러를 만들지 않으며, 끝나면 변경 파일 목록과 테스트 결과를 보고하고 멈춤. 질문으로 멈추지 않음. `04-server`, `06-mcp`도 같은 방식.
11. 체크리스트: `node scripts/check.mjs --quick`이 `CHECK 1 PASSED`, 전체 모드가 `ALL CHECKS PASSED`, 실패 시 위반 목록과 종료 코드 1. MCP `evaluate_match`, `GET /api/v1/evaluations`, `check.mjs` 지표가 서로 같음.
12. WebMCP: 플래그를 켠 Chrome에서 `await document.modelContext.getTools()`에 도구 2개가 보이고, Inspector 확장으로 `recommend_dogs` 호출이 성공. 플래그가 없는 브라우저에서도 화면이 오류 없이 동작.
13. `$ui-smoke`가 화면 4개를 돌고 `docs/screens/`에 스크린샷 4장을 저장.
14. 목 API: `node scripts/mock-api.mjs`만 띄운 상태에서 화면 4개가 끝까지 동작하고, 같은 화면이 실제 백엔드(픽스처 모드)로도 동작. 응답 필드가 `fixtures/match.sample.json`과 실제 `MatchResponse`에서 같음.
15. 키트 상태(로직·서비스·컨트롤러·`NametagTools` 없음)에서 `./gradlew compileJava --offline`의 "cannot find symbol" 목록이 `docs/CHECKLIST.md` 클래스 표의 21개와 일치하고, 표에 없는 클래스를 요구하지 않음.
16. 승인 후 이 계획 파일을 `/Users/hylee/swm/codex-impact-workshop/해커톤-준비-계획.md`로 복사해 Codex에 "이 계획의 Phase 1부터 4까지 순서대로 실행하고, 각 Phase 끝에 검증 항목을 돌려 결과를 보고해"라고 지시합니다. 8절 대본은 `docs/timeline.md`와 `prompts/`에 문장 그대로 옮깁니다.
