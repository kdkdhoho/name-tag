import {existsSync,readFileSync} from 'node:fs'; const fixtures=JSON.parse(readFileSync('fixtures/animals.json')); console.log(JSON.stringify({hookSpecificOutput:{additionalContext:`정착: fixture ${fixtures.length}건, jar ${existsSync('build/libs/nametag-0.0.1-SNAPSHOT.jar')?'있음':'없음'}, 체크리스트를 먼저 확인하세요.`}}));
